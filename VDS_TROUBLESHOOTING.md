# 🔧 Диагностика и запуск приложения на VDS

## Быстрая проверка статуса

### 1. Проверка статуса сервиса
```bash
ssh deploy@ваш_сервер
sudo systemctl status tournaments
```

**Ожидаемый результат:**
- `Active: active (running)` - приложение запущено
- `Active: inactive (dead)` - приложение остановлено

### 2. Проверка порта
```bash
netstat -tuln | grep 5000
# или
ss -tuln | grep 5000
```

**Ожидаемый результат:**
- Должна быть строка с `:5000` и статусом `LISTEN`

### 3. Проверка HTTP доступности
```bash
curl -I http://localhost:5000
```

**Ожидаемый результат:**
- HTTP код `200`, `301` или `302`

## Запуск приложения

### Способ 1: Через systemd (рекомендуется)
```bash
sudo systemctl start tournaments
sudo systemctl enable tournaments  # автозапуск при перезагрузке
sudo systemctl status tournaments   # проверка статуса
```

### Способ 2: Использование скрипта диагностики
```bash
# Скопируйте скрипт на сервер
scp check_and_start_app.sh deploy@ваш_сервер:~/quick-score/

# На сервере
ssh deploy@ваш_сервер
cd ~/quick-score
chmod +x check_and_start_app.sh
./check_and_start_app.sh
```

Скрипт автоматически:
- Проверит статус сервиса
- Проверит порт и HTTP доступность
- Попытается запустить приложение, если оно не запущено
- Покажет логи для диагностики

## Диагностика проблем

### Проблема: Сервис не запускается

**1. Проверьте логи:**
```bash
sudo journalctl -u tournaments -n 50 --no-pager
```

**2. Проверьте конфигурацию systemd:**
```bash
sudo systemctl cat tournaments
```

**3. Проверьте файл сервиса:**
```bash
cat /etc/systemd/system/tournaments.service
```

**4. Проверьте права доступа:**
```bash
cd ~/quick-score
ls -la app.py
ls -la venv/bin/activate
```

**5. Проверьте виртуальное окружение:**
```bash
cd ~/quick-score
source venv/bin/activate
python3 --version
which python3
```

### Проблема: Порт 5000 занят другим процессом

**Найдите процесс:**
```bash
sudo lsof -i :5000
# или
sudo netstat -tulpn | grep 5000
```

**Остановите процесс (если нужно):**
```bash
sudo kill -9 <PID>
```

### Проблема: Приложение запускается, но не отвечает

**1. Проверьте логи в реальном времени:**
```bash
sudo journalctl -u tournaments -f
```

**2. Проверьте, что приложение слушает правильный интерфейс:**
```bash
netstat -tuln | grep 5000
```
Должно быть `0.0.0.0:5000` или `127.0.0.1:5000`

**3. Проверьте Nginx (если используется):**
```bash
sudo systemctl status nginx
sudo nginx -t
```

**4. Проверьте файрвол:**
```bash
sudo ufw status
sudo iptables -L -n | grep 5000
```

### Проблема: Ошибки в логах

**Частые ошибки и решения:**

1. **"ModuleNotFoundError"**
   ```bash
   cd ~/quick-score
   source venv/bin/activate
   pip install -r requirements.txt
   ```

2. **"Permission denied"**
   ```bash
   sudo chown -R deploy:deploy ~/quick-score
   chmod +x ~/quick-score/app.py
   ```

3. **"Address already in use"**
   ```bash
   # Найдите и остановите процесс на порту 5000
   sudo lsof -i :5000
   sudo kill -9 <PID>
   ```

4. **"Database locked"**
   ```bash
   # Проверьте, нет ли других процессов, использующих БД
   sudo lsof ~/quick-score/instance/tournament.db
   ```

## Ручной запуск (для тестирования)

Если systemd не работает, можно запустить вручную:

```bash
cd ~/quick-score
source venv/bin/activate
python3 app.py
```

**Внимание:** Это запустит приложение в режиме отладки. Для продакшена используйте systemd.

## Перезапуск приложения

```bash
sudo systemctl restart tournaments
```

После перезапуска проверьте:
```bash
sudo systemctl status tournaments
curl -I http://localhost:5000
```

## Автозапуск при перезагрузке

Убедитесь, что сервис включен:
```bash
sudo systemctl enable tournaments
sudo systemctl is-enabled tournaments  # должно показать "enabled"
```

## Полезные команды

```bash
# Статус сервиса
sudo systemctl status tournaments

# Запуск
sudo systemctl start tournaments

# Остановка
sudo systemctl stop tournaments

# Перезапуск
sudo systemctl restart tournaments

# Логи (последние 50 строк)
sudo journalctl -u tournaments -n 50

# Логи в реальном времени
sudo journalctl -u tournaments -f

# Проверка порта
netstat -tuln | grep 5000

# Проверка процессов
ps aux | grep -E "(gunicorn|python.*app.py)"

# Проверка доступности
curl -I http://localhost:5000
```

## Контакты для поддержки

Если проблема не решается:
1. Сохраните логи: `sudo journalctl -u tournaments -n 100 > logs.txt`
2. Проверьте конфигурацию: `sudo systemctl cat tournaments > config.txt`
3. Проверьте версию: `cd ~/quick-score && git log -1`

