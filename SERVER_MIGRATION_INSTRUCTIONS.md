# 🚀 Инструкция по миграции базы данных на сервере

## 📋 Проблема
На сервере `tournament-admin@45.135.164.202` загружена последняя версия кода, но база данных `tournament.db` содержит не все необходимые поля согласно текущим моделям.

## 🛠️ Решение: Миграция базы данных

### Шаг 1: Подготовка
1. **Создайте резервную копию** базы данных на сервере:
   ```bash
   # Подключитесь к серверу
   ssh tournament-admin@45.135.164.202

   # Перейдите в директорию проекта
   cd /home/tournament-admin/quick-score

   # Создайте резервную копию
   cp instance/tournament.db instance/tournament.db.backup_$(date +%Y%m%d_%H%M%S)
   ```

### Шаг 2: Копирование скрипта миграции
Скопируйте файл `server_migration.py` на сервер в директорию `/home/deploy/quick-score/`

**Через WinSCP:**
1. Откройте WinSCP
2. Подключитесь к серверу:
   - **Протокол:** SFTP
   - **Хост:** `45.135.164.202`
   - **Пользователь:** `tournament-admin`
   - **Пароль:** (ваш пароль)
3. Скопируйте файл `server_migration.py` из локальной директории проекта в удаленную директорию `/home/tournament-admin/quick-score/`

### Шаг 3: Остановка приложения
```bash
# Остановите сервисы (проверьте названия сервисов на этом сервере)
sudo systemctl stop tournaments
sudo systemctl stop tournaments-dev

# Проверьте остановку
sudo systemctl status tournaments
```

### Шаг 4: Запуск миграции
```bash
# Перейдите в директорию проекта
cd /home/tournament-admin/quick-score

# Запустите миграцию
python3 server_migration.py
```

### Шаг 5: Проверка результатов миграции
После успешного выполнения миграции проверьте логи:
```bash
# Посмотрите последние 30 строк логов
sudo journalctl -u tournaments -n 30 --no-pager
```

### Шаг 6: Запуск приложения
```bash
# Запустите сервисы
sudo systemctl start tournaments
sudo systemctl start tournaments-dev

# Проверьте статус
sudo systemctl status tournaments
sudo systemctl status tournaments-dev
```

### Шаг 7: Тестирование
1. **Проверьте доступность сайта:** `https://quickscore.sytes.net/`
2. **Протестируйте основные функции:**
   - Создание турнира
   - Регистрация участников
   - Создание матчей
   - Ввод результатов

## 📋 Что делает миграция

### Миграция 1: Поля времени в турнирах
- Добавляет поля `start_time` и `end_time` в таблицу `tournament`
- Устанавливает значения по умолчанию: 09:00 - 17:00

### Миграция 2: Счетчик смен в розыгрышах
- Добавляет поле `swap_count` в таблицу `rally`
- Необходимо для правильного отображения стрелок в бадминтоне

### Миграция 3: Дополнительные поля матчей
- Добавляет поля для учета реального времени матчей
- Добавляет поля для хранения счета по сетам
- Добавляет поля для свободных матчей (без турнира)
- Добавляет поля для soft-delete функционала

### Миграция 4: Таблица ожидания
- Создает таблицу `waiting_list` для списка ожидания участников
- Добавляет необходимые индексы для производительности

## 🚨 Восстановление при проблемах

Если что-то пойдет не так:

```bash
# Остановите приложение
sudo systemctl stop tournaments

# Восстановите из резервной копии
cd /home/deploy/quick-score
cp instance/tournament.db.backup_YYYYMMDD_HHMMSS instance/tournament.db

# Запустите приложение
sudo systemctl start tournaments
```

## ✅ Проверочный список

- [ ] Резервная копия создана
- [ ] Скрипт `server_migration.py` скопирован на сервер
- [ ] Приложение остановлено
- [ ] Миграция выполнена успешно
- [ ] Приложение запущено
- [ ] Сайт доступен
- [ ] Основные функции работают корректно

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте логи: `sudo journalctl -u tournaments -f`
2. Свяжитесь с разработчиком с текстом ошибки
3. При необходимости восстановите из резервной копии
