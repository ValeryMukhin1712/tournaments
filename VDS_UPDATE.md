# 🚀 Обновление приложения на VDS

## Быстрое обновление (рекомендуется)

### Способ 1: Автоматический скрипт

1. **Скопируйте скрипт на VDS:**
   ```bash
   # На локальном компьютере
   scp update_vds.sh deploy@ваш_сервер:~/quick-score/
   ```

2. **Подключитесь к VDS и запустите скрипт:**
   ```bash
   ssh deploy@ваш_сервер
   cd ~/quick-score
   chmod +x update_vds.sh
   ./update_vds.sh
   ```

3. **Скрипт автоматически:**
   - Проверит текущую версию
   - Сохранит несохраненные изменения (если есть)
   - Получит обновления с GitHub
   - Применит изменения
   - Перезапустит приложение
   - Покажет новую версию

### Способ 2: Ручное обновление

1. **Подключитесь к VDS:**
   ```bash
   ssh deploy@ваш_сервер
   cd ~/quick-score
   ```

2. **Проверьте текущую версию:**
   ```bash
   git log -1 --oneline
   ```

3. **Проверьте наличие несохраненных изменений:**
   ```bash
   git status
   ```
   
   Если есть изменения, сохраните их:
   ```bash
   git stash push -m "Изменения перед обновлением"
   ```

4. **Получите последние изменения с GitHub:**
   ```bash
   git fetch origin
   ```

5. **Проверьте, что будет обновлено:**
   ```bash
   git log HEAD..origin/main --oneline
   ```

6. **Примените обновления:**
   ```bash
   git pull origin main
   ```

7. **Перезапустите приложение:**
   ```bash
   sudo systemctl restart tournaments
   ```

8. **Проверьте статус сервиса:**
   ```bash
   sudo systemctl status tournaments
   ```

9. **Проверьте логи (если нужно):**
   ```bash
   sudo journalctl -u tournaments -n 50 --no-pager
   ```

## Проверка после обновления

### 1. Проверьте версию в интерфейсе
- Войдите как системный администратор (`admin@system`)
- Откройте "Системная административная панель"
- Проверьте карточку "Информация о версии"
- Хеш коммита должен совпадать с последним коммитом на GitHub

### 2. Проверьте работу приложения
- Откройте главную страницу
- Проверьте, что турниры загружаются
- Проверьте вход в систему

### 3. Проверьте логи
```bash
sudo journalctl -u tournaments -f
```

## Откат к предыдущей версии (если что-то пошло не так)

1. **Подключитесь к VDS:**
   ```bash
   ssh deploy@ваш_сервер
   cd ~/quick-score
   ```

2. **Посмотрите историю коммитов:**
   ```bash
   git log --oneline -10
   ```

3. **Откатитесь к нужному коммиту:**
   ```bash
   git reset --hard <commit_hash>
   ```

4. **Перезапустите приложение:**
   ```bash
   sudo systemctl restart tournaments
   ```

## Решение проблем

### Проблема: Конфликты при git pull

**Решение:**
```bash
# Отмените конфликты
git merge --abort

# Или вручную разрешите конфликты
git status  # Покажет файлы с конфликтами
# Отредактируйте файлы, затем:
git add .
git commit -m "Разрешение конфликтов"
```

### Проблема: Сервис не запускается после обновления

**Решение:**
1. Проверьте логи:
   ```bash
   sudo journalctl -u tournaments -n 100 --no-pager
   ```

2. Проверьте синтаксис Python файлов:
   ```bash
   cd ~/quick-score
   python3 -m py_compile app.py
   ```

3. Проверьте зависимости:
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt
   ```

### Проблема: База данных требует миграции

**Решение:**
Приложение автоматически выполняет миграции при запуске. Если возникают ошибки:
```bash
# Проверьте логи
sudo journalctl -u tournaments -n 100 | grep -i "migration\|error"
```

Если нужно применить миграцию вручную:
```bash
cd ~/quick-score
source venv/bin/activate
python migrate_файл.py
```

## Автоматическое обновление (опционально)

Можно настроить автоматическое обновление через cron:

1. **Создайте скрипт:**
   ```bash
   nano ~/quick-score/auto_update.sh
   ```
   
   Содержимое:
   ```bash
   #!/bin/bash
   cd ~/quick-score
   git fetch origin
   LOCAL=$(git rev-parse HEAD)
   REMOTE=$(git rev-parse origin/main)
   if [ "$LOCAL" != "$REMOTE" ]; then
       git pull origin main
       sudo systemctl restart tournaments
   fi
   ```

2. **Сделайте исполняемым:**
   ```bash
   chmod +x ~/quick-score/auto_update.sh
   ```

3. **Добавьте в crontab (обновление каждый час):**
   ```bash
   crontab -e
   # Добавьте строку:
   0 * * * * /home/deploy/quick-score/auto_update.sh >> /home/deploy/quick-score/update.log 2>&1
   ```

## Полезные команды

**Проверка разницы с GitHub:**
```bash
git fetch origin
git diff HEAD origin/main
```

**Просмотр истории изменений:**
```bash
git log --oneline --graph -20
```

**Отмена локальных изменений (осторожно!):**
```bash
git reset --hard HEAD
```

**Восстановление изменений из stash:**
```bash
git stash list
git stash pop
```

---

**Важно:** 
- Всегда проверяйте работу приложения после обновления
- Сохраняйте бэкапы базы данных перед крупными обновлениями
- Тестируйте обновления на тестовой среде перед продакшн












