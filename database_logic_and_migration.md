# 📊 Логика взаимодействия с базой данных

## 🗄️ Архитектура базы данных

### Тип базы данных
Проект использует **SQLite** - файловую базу данных, которая хранится в одном файле.

### Расположение файла БД
- **Локально**: `instance/tournament.db`
- **На сервере (VDS)**: `~/quick-score/instance/tournament.db` (или `/home/deploy/quick-score/instance/tournament.db`)

### Конфигурация подключения

База данных настраивается через конфигурационные файлы:

**`config.py`**:
```python
SQLALCHEMY_DATABASE_URI = os.environ.get('DATABASE_URL') or 'sqlite:///instance/tournament.db'
```

**`app.py`** (переопределение для совместимости):
```python
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///tournament.db'
```

### Структура моделей

База данных содержит следующие таблицы (модели):

1. **User** - пользователи системы (администраторы, судьи)
2. **Tournament** - турниры
3. **Participant** - участники турниров
4. **Match** - матчи
5. **MatchLog** - логи матчей
6. **Rally** - розыгрыши (очки в матчах)
7. **Player** - игроки (справочник)
8. **Token** - токены для создания турниров
9. **Notification** - уведомления
10. **WaitingList** - списки ожидания
11. **Settings** - настройки системы
12. **UserActivity** - активность пользователей

## 🔄 Инициализация базы данных

### Автоматическая инициализация

При запуске приложения вызывается функция `init_db()` в `app.py`:

```python
def init_db():
    with app.app_context():
        # Создаем все таблицы
        db.create_all()
        
        # Применяем миграции (добавление новых колонок)
        # ...
        
        # Создаем администратора по умолчанию
        # admin/adm555
```

### Ручная инициализация

Для создания БД с нуля можно использовать:

```bash
python init_db.py
```

Этот скрипт:
- Создает все таблицы
- Создает администратора (admin/adm555)
- Инициализирует настройки по умолчанию

## 🔧 Работа с базой данных

### ORM (SQLAlchemy)

Проект использует **Flask-SQLAlchemy** для работы с БД через объектно-реляционное отображение:

```python
from models import db, Tournament, Match, Rally

# Создание записи
tournament = Tournament(name="Чемпионат", ...)
db.session.add(tournament)
db.session.commit()

# Чтение
tournaments = Tournament.query.all()
tournament = Tournament.query.get(id)

# Обновление
tournament.name = "Новое название"
db.session.commit()

# Удаление
db.session.delete(tournament)
db.session.commit()
```

### Миграции

При добавлении новых полей в модели применяются миграции:

**Пример миграции** (`migrate_add_swap_count_to_rally.py`):
```python
# Проверяем наличие колонки
# Если нет - добавляем через ALTER TABLE
conn.execute(text("ALTER TABLE rally ADD COLUMN swap_count INTEGER DEFAULT 0"))
```

## 📦 Перенос базы данных

### ⚠️ ВАЖНО: Перед переносом

**Перенос БД ЗАМЕНИТ существующую БД на целевом сервере!** Все данные будут потеряны!

### 🔄 Перенос с локального компьютера на сервер (VDS)

#### Шаг 1: Создание бэкапа на сервере

```bash
# Подключитесь к серверу
ssh deploy@ваш_сервер

# Создайте бэкап текущей БД
cd ~/quick-score
mkdir -p ~/backups
cp instance/tournament.db ~/backups/tournament_backup_$(date +%Y%m%d_%H%M%S).db
```

#### Шаг 2: Остановка приложения на сервере

```bash
sudo systemctl stop tournaments
```

#### Шаг 3: Копирование БД с локального компьютера

**Вариант A: Через SCP (рекомендуется)**

```bash
# На локальном компьютере
scp instance/tournament.db deploy@ваш_сервер:/home/deploy/quick-score/instance/tournament.db
```

**Вариант B: Через архив**

```bash
# На локальном компьютере
tar -czf tournament_db_backup.tar.gz instance/tournament.db
scp tournament_db_backup.tar.gz deploy@ваш_сервер:/tmp/

# На сервере
cd ~/quick-score
tar -xzf /tmp/tournament_db_backup.tar.gz -C instance/
```

#### Шаг 4: Установка прав доступа на сервере

**⚠️ ВАЖНО: Эти команды выполняются НА СЕРВЕРЕ (VDS), а не локально на Windows!**

**Где выполнять:**
- На сервере VDS после подключения через SSH
- В директории проекта: `~/quick-score` или `/home/deploy/quick-score`

**Полная последовательность команд:**

```bash
# 1. Подключитесь к серверу через SSH (с локального компьютера)
ssh deploy@ваш_сервер

# 2. Перейдите в директорию проекта (на сервере)
cd ~/quick-score
# или полный путь:
# cd /home/deploy/quick-score

# 3. Установите права доступа на файл БД (на сервере)
chmod 644 instance/tournament.db
chown deploy:deploy instance/tournament.db

# 4. Проверьте права (опционально, на сервере)
ls -lh instance/tournament.db
# Должно показать: -rw-r--r-- 1 deploy deploy ... tournament.db
```

**Пояснение команд:**
- `chmod 644` - устанавливает права: владелец может читать/писать (6), группа и остальные - только читать (4)
- `chown deploy:deploy` - устанавливает владельца и группу файла на `deploy`
- `instance/tournament.db` - путь к файлу БД относительно текущей директории (`~/quick-score`)

#### Шаг 5: Запуск приложения

```bash
sudo systemctl start tournaments
sudo systemctl status tournaments
```

### 🔄 Перенос с сервера на локальный компьютер

#### Шаг 1: Создание бэкапа локальной БД

```bash
# На локальном компьютере
cp instance/tournament.db instance/tournament_backup_$(date +%Y%m%d_%H%M%S).db
```

#### Шаг 2: Копирование БД с сервера

```bash
# На локальном компьютере
scp deploy@ваш_сервер:/home/deploy/quick-score/instance/tournament.db instance/tournament.db
```

#### Шаг 3: Проверка

```bash
# Запустите приложение локально
python app.py

# Проверьте работу в браузере
```

### 🔄 Синхронизация структуры БД (без потери данных)

Если нужно только синхронизировать структуру (добавить новые колонки), а не заменять всю БД:

#### На сервере:

```bash
ssh deploy@ваш_сервер
cd ~/quick-score
source venv/bin/activate

# Примените миграцию
python migrate_add_swap_count_to_rally.py

# Проверьте структуру
python -c "from app import app, db; from sqlalchemy import text; with app.app_context(): conn = db.engine.connect(); result = conn.execute(text('PRAGMA table_info(rally)')); cols = result.fetchall(); print('Структура таблицы rally:'); [print(f'  {c[1]} ({c[2]})') for c in cols]"

# Перезапустите приложение
sudo systemctl restart tournaments
```

## 🔍 Проверка базы данных

### Проверка структуры таблицы

```bash
python -c "from app import app, db; from sqlalchemy import text; with app.app_context(): conn = db.engine.connect(); result = conn.execute(text('PRAGMA table_info(rally)')); cols = result.fetchall(); print('Структура таблицы rally:'); [print(f'  {c[1]} ({c[2]})') for c in cols]"
```

### Проверка размера БД

```bash
# Локально
ls -lh instance/tournament.db

# На сервере
ls -lh ~/quick-score/instance/tournament.db
```

### Просмотр данных через SQLite CLI

```bash
# Локально
sqlite3 instance/tournament.db
.tables
SELECT * FROM tournament;
.quit

# На сервере
sqlite3 ~/quick-score/instance/tournament.db
.tables
SELECT * FROM tournament;
.quit
```

## 🔄 Откат изменений

Если после переноса возникли проблемы:

```bash
# На сервере
cd ~/quick-score
sudo systemctl stop tournaments

# Восстановите бэкап
cp ~/backups/tournament_backup_YYYYMMDD_HHMMSS.db instance/tournament.db

# Установите права (на сервере, в директории ~/quick-score)
chmod 644 instance/tournament.db
chown deploy:deploy instance/tournament.db

# Запустите приложение
sudo systemctl start tournaments
```

## 📝 Рекомендации

### Когда переносить БД полностью:

1. ✅ Локальная БД содержит актуальные данные
2. ✅ Серверная БД устарела или повреждена
3. ✅ Нужно полностью синхронизировать данные

### Когда применять миграции:

1. ✅ Нужно только добавить новые колонки
2. ✅ Данные на сервере важны и их нужно сохранить
3. ✅ Структура БД отличается только новыми полями

### Бэкапы:

- **Всегда** создавайте бэкап перед переносом БД
- Храните бэкапы в отдельной директории (`~/backups/`)
- Используйте дату и время в имени файла бэкапа
- Регулярно проверяйте целостность бэкапов

### Безопасность:

- БД содержит чувствительные данные (пароли, токены)
- Не коммитьте файл БД в Git (он в `.gitignore`)
- Используйте безопасное соединение (SSH/SCP) для переноса
- Устанавливайте правильные права доступа (644 для файла, владелец - пользователь приложения)

## 🛠️ Устранение проблем

### Ошибка: "no such column"

**Причина**: Структура БД на сервере отличается от локальной (отсутствуют новые колонки).

**Решение**: Примените миграции или перенесите БД полностью.

### Ошибка: "database is locked"

**Причина**: Приложение использует БД, или другой процесс заблокировал файл.

**Решение**: 
1. Остановите приложение: `sudo systemctl stop tournaments`
2. Подождите несколько секунд
3. Повторите операцию

### Ошибка: "permission denied"

**Причина**: Неправильные права доступа к файлу БД.

**Решение (на сервере):**
```bash
# На сервере, в директории ~/quick-score
chmod 644 instance/tournament.db
chown deploy:deploy instance/tournament.db
```

## 🔀 Слияние баз данных (Merge)

### Объединение данных из двух БД

Если нужно объединить данные из локальной и серверной БД (не заменяя, а суммируя данные), можно использовать скрипт `merge_databases.py`.

### Использование скрипта слияния

#### Базовое использование:

```bash
python merge_databases.py \
  --source instance/tournament.db \
  --target instance/tournament_server.db \
  --output instance/tournament_merged.db
```

#### Параметры:

- `--source`: путь к исходной БД (локальная)
- `--target`: путь к целевой БД (серверная)
- `--output`: путь к результирующей БД (по умолчанию: `instance/tournament_merged.db`)
- `--no-backup`: не создавать бэкап целевой БД перед слиянием

### Как работает слияние:

1. **Независимые таблицы** (User, Player, Settings, Token и др.):
   - Объединяются все записи из обеих БД
   - Дубликаты определяются по уникальным полям (username, name, token и т.д.)
   - При конфликте используется запись из целевой БД

2. **Зависимые таблицы** (Tournament, Participant, Match, Rally):
   - Объединяются все записи
   - Автоматически обновляются foreign keys (ID связей)
   - ID пересчитываются для избежания конфликтов

3. **Порядок обработки**:
   - Сначала независимые таблицы
   - Затем зависимые в порядке иерархии:
     - Tournament → Participant → Match → Rally

### Пример слияния локальной и серверной БД:

#### Шаг 1: Скачайте серверную БД

```bash
# Скачайте БД с сервера
scp deploy@ваш_сервер:/home/deploy/quick-score/instance/tournament.db instance/tournament_server.db
```

#### Шаг 2: Выполните слияние

```bash
python merge_databases.py \
  --source instance/tournament.db \
  --target instance/tournament_server.db \
  --output instance/tournament_merged.db
```

#### Шаг 3: Проверьте результат

```bash
# Проверьте размер и структуру
ls -lh instance/tournament_merged.db

# Просмотрите данные
sqlite3 instance/tournament_merged.db
.tables
SELECT COUNT(*) FROM tournament;
SELECT COUNT(*) FROM match;
.quit
```

#### Шаг 4: Используйте объединенную БД

```bash
# Создайте бэкап текущей БД
cp instance/tournament.db instance/tournament_backup_$(date +%Y%m%d_%H%M%S).db

# Замените текущую БД на объединенную
cp instance/tournament_merged.db instance/tournament.db
```

### ⚠️ Важные замечания:

1. **Бэкап**: Скрипт автоматически создает бэкап целевой БД перед слиянием
2. **ID конфликты**: Автоматически разрешаются путем пересчета ID
3. **Foreign keys**: Автоматически обновляются согласно новым ID
4. **Дубликаты**: Определяются по уникальным полям (name для Tournament, username для User и т.д.)
5. **Тестирование**: Всегда проверяйте результат перед использованием в продакшене

### Ограничения:

- Скрипт работает только с SQLite
- Сложные связи между таблицами могут потребовать ручной проверки
- Рекомендуется тестировать на копиях БД перед применением к реальным данным

## 📚 Дополнительные ресурсы

- Документация SQLite: https://www.sqlite.org/docs.html
- Документация Flask-SQLAlchemy: https://flask-sqlalchemy.palletsprojects.com/
- Файл с инструкциями по переносу: `transfer_database_to_vds.md`
- Скрипт слияния БД: `merge_databases.py`

