# 📱 Руководство по подключению Telegram через QR-код

## 🎯 Что это?

Теперь участники турниров могут **автоматически подключить Telegram** для получения уведомлений, просто отсканировав QR-код! Больше не нужно вручную вводить Chat ID или username.

---

## 🚀 Как это работает?

### Для участника:

1. **Подает заявку на турнир** через веб-приложение
2. **Видит QR-код** сразу после успешной подачи заявки
3. **Сканирует QR-код** камерой телефона
4. **Нажимает START** в открывшемся Telegram боте
5. **Готово!** Telegram автоматически подключен к заявке

### Для системы:

1. При подаче заявки генерируется **уникальный токен**
2. QR-код содержит **Telegram Deep Link** с токеном: `t.me/bot?start=TOKEN`
3. Когда пользователь переходит по ссылке, бот получает команду `/start TOKEN`
4. Бот автоматически **связывает Chat ID** пользователя с заявкой через API
5. Пользователь начинает **получать уведомления**

---

## 🛠️ Техническая реализация

### 1. База данных

Добавлено поле `telegram_token` в таблицу `waiting_list`:

```sql
ALTER TABLE waiting_list ADD COLUMN telegram_token VARCHAR(100);
CREATE UNIQUE INDEX idx_waiting_list_telegram_token ON waiting_list(telegram_token);
```

### 2. Генерация токена и QR-кода

**Файл:** `utils/qr_generator.py`

- `generate_telegram_token()` - генерирует уникальный токен длиной 32 символа
- `generate_qr_code(token)` - создает QR-код с Deep Link
- Возвращает base64-encoded PNG изображение

### 3. API Endpoints

#### a) Подача заявки (модифицирован)
```
POST /api/join-request
```
**Ответ:**
```json
{
  "success": true,
  "message": "Заявка успешно подана",
  "qr_code": "data:image/png;base64,...",
  "telegram_token": "abc123..."
}
```

#### b) Связывание токена (новый)
```
POST /api/telegram/link-token
```
**Запрос:**
```json
{
  "token": "abc123...",
  "chat_id": "123456789"
}
```
**Ответ:**
```json
{
  "success": true,
  "message": "Telegram успешно подключен",
  "participant_name": "Иван",
  "tournament_id": 17
}
```

### 4. Telegram Bot Handler

**Файл:** `telegram_bot_handler.py`

**Обработка Deep Link:**
```python
if text.startswith('/start'):
    parts = text.split()
    if len(parts) > 1:
        token = parts[1]
        success = self.link_token_to_chat(token, chat_id)
        # Отправка сообщения пользователю
```

**Метод привязки:**
```python
def link_token_to_chat(self, token, chat_id):
    url = f"{self.app_api_url}/api/telegram/link-token"
    response = requests.post(url, json={'token': token, 'chat_id': chat_id})
    return response.json().get('success')
```

### 5. Frontend

**Файл:** `templates/tournaments.html`

**Функция показа QR-кода:**
```javascript
function showQRCodeModal(participantName, qrCode, tournamentId) {
    // Создает модальное окно с QR-кодом
    // Показывает инструкции для пользователя
}
```

**Обработка ответа:**
```javascript
if (data.success && data.qr_code) {
    showQRCodeModal(participantName, data.qr_code, tournamentId);
}
```

---

## ⚙️ Настройка

### 1. Узнайте username вашего бота

1. Откройте Telegram
2. Найдите вашего бота (созданного через @BotFather)
3. Скопируйте username (например: `quickscore_bot`)

### 2. Обновите `utils/qr_generator.py`

Замените строку:
```python
bot_username = "quickscore_feedback_bot"  # ЗАМЕНИТЕ на реальный username вашего бота!
```
на:
```python
bot_username = "ваш_бот_username"  # Без @
```

### 3. Запустите Telegram Bot Handler

```bash
python telegram_bot_handler.py
```

**Важно:** Бот должен быть запущен одновременно с Flask приложением!

---

## 📦 Зависимости

Добавлены новые библиотеки в `requirements.txt`:

```
qrcode==7.4.2
Pillow==10.0.0
```

Установка:
```bash
pip install -r requirements.txt
```

---

## 🧪 Тестирование

### 1. Проверка генерации QR-кода

```python
from utils.qr_generator import generate_telegram_token, generate_qr_code

token = generate_telegram_token()
print(f"Токен: {token}")

qr_code = generate_qr_code(token, "ваш_бот_username")
print(f"QR-код сгенерирован: {len(qr_code)} байт")
```

### 2. Тестовая подача заявки

1. Откройте приложение
2. Выберите турнир со статусом "Регистрация"
3. Нажмите "Подать заявку"
4. Заполните форму
5. После отправки должен появиться QR-код

### 3. Тестирование бота

1. Запустите бота: `python telegram_bot_handler.py`
2. Отсканируйте QR-код телефоном
3. Нажмите START в Telegram
4. Бот должен отправить: "✅ Отлично! Ваш Telegram успешно подключен..."
5. Проверьте логи бота - должна быть запись о привязке токена

---

## 🔍 Отладка

### Проблема: QR-код не генерируется

**Решение:**
- Проверьте, установлены ли библиотеки: `pip list | grep qrcode`
- Проверьте логи Flask: ищите "QR-код сгенерирован" или ошибки

### Проблема: Бот не отвечает на QR-код

**Решение:**
1. Убедитесь, что бот запущен: `python telegram_bot_handler.py`
2. Проверьте username бота в `utils/qr_generator.py`
3. Проверьте, что Flask приложение запущено и доступно на `http://127.0.0.1:5000`

### Проблема: "Ошибка подключения" в боте

**Решение:**
- Проверьте, что Flask приложение запущено
- Проверьте логи Flask: ищите запросы к `/api/telegram/link-token`
- Убедитесь, что токен не был использован ранее (каждый токен одноразовый)

---

## 🎨 Кастомизация

### Изменение дизайна QR-кода

В `utils/qr_generator.py`:

```python
qr = qrcode.QRCode(
    version=1,              # Размер (1-40)
    error_correction=qrcode.constants.ERROR_CORRECT_L,  # Уровень коррекции
    box_size=10,            # Размер пикселя
    border=4,               # Толщина рамки
)
```

### Изменение текста в модальном окне

В `templates/tournaments.html` - функция `showQRCodeModal()`:

```javascript
<p class="text-muted small mb-3">
    Отсканируйте QR-код телефоном...  // Ваш текст
</p>
```

---

## 📊 Преимущества

| До | После |
|---|---|
| ❌ Участник вводит Chat ID вручную | ✅ Участник сканирует QR-код |
| ❌ Возможны ошибки ввода | ✅ Автоматическое подключение |
| ❌ Нужно знать, что такое Chat ID | ✅ Интуитивно понятно |
| ❌ Участник может не указать Telegram | ✅ QR-код мотивирует подключить |
| ❌ Сложно для неопытных пользователей | ✅ Просто для всех |

---

## 🚀 Деплой на VDS

### 1. Обновите код

```bash
cd /path/to/your/app
git pull origin main
```

### 2. Установите зависимости

```bash
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Запустите миграцию

```bash
python migrate_add_telegram_token.py
```

### 4. Обновите username бота

Отредактируйте `utils/qr_generator.py` (см. раздел "Настройка")

### 5. Перезапустите сервисы

```bash
sudo systemctl restart your-flask-app
```

### 6. Запустите бота

Создайте systemd service для бота или используйте screen/tmux:

```bash
screen -S telegram_bot
python telegram_bot_handler.py
# Ctrl+A, D для отсоединения
```

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи Flask: `tail -f app.log`
2. Проверьте логи бота
3. Проверьте, что оба процесса запущены:
   - Flask приложение
   - Telegram Bot Handler

---

## 📝 Changelog

### v1.0 (28.10.2025)

✅ Добавлена генерация QR-кодов при подаче заявки
✅ Добавлен API endpoint для связывания токенов
✅ Обновлен Telegram Bot Handler с поддержкой Deep Links
✅ Добавлено модальное окно с QR-кодом
✅ Добавлена миграция базы данных
✅ Создана документация

---

**🎉 Готово! Теперь ваше приложение поддерживает автоматическое подключение Telegram через QR-код!**

