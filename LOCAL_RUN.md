# 🖥️ Запуск приложения локально на Windows

## Важно!

**Убедитесь, что вы работаете в локальном терминале Windows (PowerShell или CMD), а НЕ в SSH сессии к серверу!**

Если вы видите в терминале `deploy@132742:~$` или подобное - это означает, что вы подключены к удаленному серверу. Вам нужно открыть новый терминал на вашей локальной Windows машине.

## Способ 1: Использование скрипта run_local.ps1

1. Откройте **PowerShell** на вашей Windows машине (НЕ через SSH)
2. Перейдите в директорию проекта:
   ```powershell
   cd C:\Cursor\Tournaments_v.1
   ```
3. Запустите скрипт:
   ```powershell
   .\run_local.ps1
   ```

## Способ 2: Ручной запуск

1. Откройте **PowerShell** на вашей Windows машине
2. Перейдите в директорию проекта:
   ```powershell
   cd C:\Cursor\Tournaments_v.1
   ```
3. Активируйте виртуальное окружение:
   ```powershell
   .\venv\Scripts\Activate.ps1
   ```
   Если виртуального окружения нет:
   ```powershell
   python -m venv venv
   .\venv\Scripts\Activate.ps1
   pip install -r requirements.txt
   ```
4. Проверьте наличие файла `.env`:
   ```powershell
   if (-not (Test-Path ".env")) {
       Copy-Item "env.example" ".env"
   }
   ```
5. Инициализируйте базу данных (если нужно):
   ```powershell
   python init_db.py
   ```
6. Запустите приложение:
   ```powershell
   python app.py
   ```

## Проверка

После запуска приложение будет доступно по адресу:
- **http://localhost:5000**
- **http://127.0.0.1:5000**

Откройте браузер и перейдите по одному из этих адресов.

## Остановка приложения

Нажмите `Ctrl+C` в терминале, где запущено приложение.

## Решение проблем

### Ошибка "Execution Policy"

Если появляется ошибка о политике выполнения:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Порт 5000 занят

Если порт 5000 занят другим приложением:
1. Найдите процесс, использующий порт:
   ```powershell
   netstat -ano | findstr :5000
   ```
2. Остановите процесс или измените порт в `app.py`

### Виртуальное окружение не активируется

Убедитесь, что вы используете правильный путь:
```powershell
# Для PowerShell
.\venv\Scripts\Activate.ps1

# Для CMD
venv\Scripts\activate.bat
```

---

**Помните**: Если вы видите `deploy@132742` в терминале - вы подключены к серверу, а не работаете локально!

