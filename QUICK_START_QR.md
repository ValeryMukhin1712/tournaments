# 🚀 Быстрый старт: QR-коды для Telegram

## ✅ Что сделано?

Теперь участники могут **подключить Telegram, просто отсканировав QR-код**!

---

## 📋 Что нужно сделать (3 шага):

### ШАГ 1: Узнайте username вашего Telegram бота

1. Откройте Telegram
2. Найдите вашего бота (который вы создали через @BotFather)
3. Посмотрите username (например: `quickscore_bot`)

### ШАГ 2: Обновите username бота в коде

Откройте файл `utils/qr_generator.py` и найдите строку:

```python
bot_username = "quickscore_feedback_bot"  # ЗАМЕНИТЕ на реальный username вашего бота!
```

Замените на:

```python
bot_username = "ваш_бот_username"  # Без символа @
```

### ШАГ 3: Готово!

Теперь можно тестировать:

1. Убедитесь, что Flask приложение запущено (`python app.py`)
2. Запустите Telegram бота (`python telegram_bot_handler.py`)
3. Подайте заявку на турнир через веб-интерфейс
4. Отсканируйте QR-код, который появится после отправки заявки
5. Нажмите START в Telegram боте

---

## 📱 Как это выглядит для участника?

1. **Участник подает заявку** → вводит имя и уровень
2. **Появляется окно** с QR-кодом и инструкцией
3. **Сканирует QR-код** камерой телефона
4. **Открывается Telegram** → нажимает START
5. **Бот сообщает:** "✅ Ваш Telegram успешно подключен!"
6. **Готово!** Участник будет получать уведомления

---

## 🔧 Настройка для VDS

После деплоя на сервер:

```bash
# 1. Обновите код
cd /path/to/your/app
git pull origin main

# 2. Установите новые библиотеки
pip install -r requirements.txt

# 3. Запустите миграцию
python migrate_add_telegram_token.py

# 4. Обновите username бота в utils/qr_generator.py
nano utils/qr_generator.py

# 5. Перезапустите приложение
sudo systemctl restart your-app-name

# 6. Запустите бота (в фоне)
screen -S telegram_bot
python telegram_bot_handler.py
# Нажмите Ctrl+A, затем D для отсоединения
```

---

## ❓ Частые вопросы

**Q: Где взять username бота?**
A: В Telegram найдите вашего бота и посмотрите в профиле (без символа @)

**Q: Нужно ли запускать бота отдельно?**
A: Да! Бот должен работать одновременно с Flask приложением.

**Q: Можно ли все еще вводить Chat ID вручную?**
A: Да, это по-прежнему работает как fallback вариант.

**Q: QR-код одноразовый?**
A: Да, каждый QR-код можно использовать только один раз. При повторной заявке будет новый QR-код.

---

## 🎯 Преимущества

- ✅ Никаких ошибок ввода
- ✅ Работает даже если пользователь не знает, что такое Chat ID
- ✅ Быстро и удобно
- ✅ Современный UX

---

## 📞 Если что-то не работает

1. **QR-код не появляется?**
   - Проверьте логи Flask: `tail -f app.log`
   - Ищите ошибки связанные с qrcode

2. **Бот не отвечает?**
   - Убедитесь, что `telegram_bot_handler.py` запущен
   - Проверьте, что username в `utils/qr_generator.py` правильный

3. **"Ошибка подключения" в боте?**
   - Проверьте, что Flask приложение доступно на `http://127.0.0.1:5000`
   - Если на VDS, измените URL в telegram_bot_handler.py

---

**🎉 Готово! Теперь участники могут подключать Telegram одним сканом!**















