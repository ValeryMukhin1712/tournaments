# 📋 Проверка версии приложения на VDS

## Способ 1: Через веб-интерфейс (Рекомендуется)

1. **Откройте системную админ-панель:**
   - Войдите как системный администратор (`admin@system`)
   - Перейдите на страницу "Системная административная панель"
   - В верхней части страницы будет карточка **"Информация о версии"**

2. **Информация, которая отображается:**
   - Git коммит (первые 7 символов хеша)
   - Сообщение коммита
   - Ветка (обычно `main`)
   - Дата коммита
   - Дата последнего изменения `app.py`
   - Статус изменений (есть ли несохраненные изменения)

3. **Сравнение с GitHub:**
   - Скопируйте хеш коммита из интерфейса
   - Проверьте на GitHub в репозитории, что этот коммит существует
   - Или сравните сообщение коммита с последним коммитом на GitHub

## Способ 2: Через SSH и скрипт

1. **Подключитесь к VDS:**
   ```bash
   ssh deploy@ваш_сервер
   ```

2. **Запустите скрипт проверки версии:**
   ```bash
   cd ~/quick-score
   bash check_version.sh
   ```

3. **Скрипт покажет:**
   - Текущий коммит на сервере
   - Дату последнего обновления
   - Последние 5 коммитов
   - Сравнение с GitHub (если есть доступ к интернету)

## Способ 3: Через SSH вручную

1. **Подключитесь к VDS:**
   ```bash
   ssh deploy@ваш_сервер
   cd ~/quick-score
   ```

2. **Проверьте текущий коммит:**
   ```bash
   git log -1 --pretty=format:"%H - %s (%ar)"
   ```

3. **Проверьте последние коммиты:**
   ```bash
   git log --oneline -5
   ```

4. **Сравните с GitHub:**
   ```bash
   git fetch origin
   git log HEAD..origin/main --oneline  # Что есть на GitHub, но нет локально
   git log origin/main..HEAD --oneline  # Что есть локально, но нет на GitHub
   ```

5. **Проверьте, есть ли несохраненные изменения:**
   ```bash
   git status
   ```

## Способ 4: Через API (для автоматизации)

**Endpoint:** `GET /api/admin/version`

**Требования:**
- Авторизация системного администратора (`admin@system`)
- CSRF токен

**Пример запроса:**
```bash
curl -X GET http://ваш_домен/api/admin/version \
  -H "Cookie: session=ваш_сессионный_токен" \
  -H "X-CSRFToken: ваш_csrf_токен"
```

**Пример ответа:**
```json
{
  "success": true,
  "version": {
    "git_commit_hash": "335615d",
    "git_commit_message": "Обновления: документация, проверочные скрипты и улучшения API",
    "git_commit_date": "2025-11-01 13:30:00 +0300",
    "git_branch": "main",
    "has_uncommitted_changes": false,
    "app_file_modified": "2025-11-01 13:29:45"
  }
}
```

## Обновление версии на VDS

Если версия на VDS отличается от GitHub:

1. **Подключитесь к VDS:**
   ```bash
   ssh deploy@ваш_сервер
   cd ~/quick-score
   ```

2. **Получите последние изменения:**
   ```bash
   git fetch origin
   git pull origin main
   ```

3. **Перезапустите приложение:**
   ```bash
   sudo systemctl restart tournaments
   ```

4. **Проверьте логи:**
   ```bash
   sudo journalctl -u tournaments -n 50 --no-pager
   ```

## Сравнение версий

### Локальная разработка vs VDS
- **Локально:** Посмотрите последний коммит: `git log -1`
- **На VDS:** Используйте веб-интерфейс или SSH команды выше
- **Сравнение:** Хеши коммитов должны совпадать, если версии одинаковые

### VDS vs GitHub
- Если коммит на VDS есть на GitHub → версия актуальна
- Если на GitHub есть новые коммиты → нужен `git pull`
- Если на VDS есть локальные изменения → нужно решить, что с ними делать

## Полезные команды

**Проверка статуса git:**
```bash
cd ~/quick-score
git status
```

**Просмотр истории:**
```bash
git log --oneline -10
```

**Проверка разницы с GitHub:**
```bash
git fetch origin
git diff HEAD origin/main
```

**Дата последнего изменения файла:**
```bash
ls -lh app.py
```

---

**Примечание:** Скрипт `check_version.sh` должен быть скопирован на VDS в директорию `~/quick-score/` и иметь права на выполнение (`chmod +x check_version.sh`).













