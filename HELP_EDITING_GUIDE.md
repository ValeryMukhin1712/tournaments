# 📝 Руководство по безопасному редактированию Help-файла

## 🎯 Обзор

Help-контент в приложении хранится в двух местах:
1. **`templates/base.html`** (строки 808-898) - основной файл, используется в приложении
2. **`help_content.html`** - отдельный файл для удобного редактирования

## ✅ Безопасный способ редактирования

### Способ 1: Использование скрипта (РЕКОМЕНДУЕТСЯ)

#### Шаг 1: Подготовка к редактированию
```powershell
python edit_help_safe.py
```

Скрипт автоматически:
- ✅ Создаст резервные копии всех файлов
- ✅ Извлечет Help-контент из `base.html` в `help_content.html` (если его нет)
- ✅ Удалит иконки FontAwesome для упрощения редактирования

#### Шаг 2: Редактирование
1. Откройте файл **`help_content.html`** в любом текстовом редакторе или браузере
2. Отредактируйте содержимое между тегами `<body>` и `</body>`
3. **ВАЖНО:** Не редактируйте:
   - Теги `<html>`, `<head>`, `<body>`
   - Стили CSS в блоке `<style>`
   - Только редактируйте контент внутри `<body>...</body>`

#### Шаг 3: Синхронизация изменений
После редактирования синхронизируйте изменения обратно в `base.html`:
```powershell
python edit_help_safe.py --sync
```

Скрипт автоматически:
- ✅ Создаст резервную копию `base.html`
- ✅ Добавит иконки FontAwesome обратно
- ✅ Обновит Help-контент в `base.html`

#### Шаг 4: Проверка
1. Перезапустите приложение (если оно запущено)
2. Откройте приложение в браузере
3. Нажмите кнопку "Помощь" (иконка вопроса)
4. Проверьте, что изменения отображаются корректно

### Способ 2: Прямое редактирование base.html (для опытных)

⚠️ **ВНИМАНИЕ:** Этот способ требует осторожности!

1. **Создайте резервную копию:**
   ```powershell
   Copy-Item templates\base.html templates\base.html.backup
   ```

2. **Откройте файл:**
   ```
   templates/base.html
   ```

3. **Найдите блок Help-контента:**
   - Ищите строку: `<div class="help-content">` (примерно строка 808)
   - Редактируйте содержимое до закрывающего тега `</div>` (примерно строка 898)

4. **Сохраните файл**

5. **Проверьте изменения** в браузере

## 📁 Структура файлов

```
backups/                          # Папка с резервными копиями (создается автоматически)
  ├── base.html.backup_YYYYMMDD_HHMMSS
  └── help_content.html.backup_YYYYMMDD_HHMMSS

templates/
  └── base.html                   # Основной файл (строки 808-898 - Help-контент)

help_content.html                 # Файл для редактирования

edit_help_safe.py                 # Скрипт для безопасного редактирования
```

## 🔍 Где находится Help-контент в base.html?

Help-контент находится в модальном окне:
```html
<div class="modal fade" id="helpModal" ...>
    <div class="modal-body">
        <div class="help-content">
            <!-- ВОТ ЗДЕСЬ ВАШ HELP-КОНТЕНТ -->
        </div>
    </div>
</div>
```

## 📋 Форматирование Help-контента

### Поддерживаемые HTML-теги:
- `<h4>` - Заголовок первого уровня (зеленый)
- `<h5>` - Заголовок второго уровня (синий)
- `<h6>` - Заголовок третьего уровня (серый)
- `<p>` - Параграф
- `<ul>`, `<ol>` - Списки
- `<li>` - Элемент списка
- `<strong>` - Жирный текст (синий)
- `<p class="lead">` - Выделенный параграф (курсив)

### Иконки FontAwesome

В `base.html` используются иконки:
- `<i class="fas fa-trophy me-2"></i>` - для заголовков о турнирах
- `<i class="fas fa-users me-2"></i>` - для ролей пользователей
- `<i class="fas fa-crown me-2"></i>` - для администратора
- `<i class="fas fa-eye me-2"></i>` - для просмотра
- `<i class="fas fa-list me-2"></i>` - для списков
- `<i class="fas fa-info-circle me-2"></i>` - для информации

**Примечание:** В `help_content.html` иконки удалены для упрощения редактирования. Скрипт автоматически добавит их обратно при синхронизации.

## ⚠️ Важные предупреждения

1. **Всегда создавайте резервные копии** перед редактированием
2. **Не редактируйте** структуру HTML (теги, классы, стили)
3. **Проверяйте изменения** в браузере после редактирования
4. **Не удаляйте** закрывающие теги `</div>`, `</ul>`, `</li>` и т.д.
5. **Сохраняйте кодировку UTF-8** при сохранении файлов

## 🔄 Восстановление из резервной копии

Если что-то пошло не так:

1. Найдите нужную резервную копию в папке `backups/`
2. Скопируйте её обратно:
   ```powershell
   Copy-Item backups\base.html.backup_YYYYMMDD_HHMMSS templates\base.html
   ```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что файлы имеют кодировку UTF-8
2. Убедитесь, что все HTML-теги закрыты правильно
3. Проверьте консоль браузера на наличие ошибок JavaScript
4. Восстановите из резервной копии, если нужно

---

**Удачного редактирования! 🎉**


