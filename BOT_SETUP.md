# 🤖 Настройка и запуск Telegram бота для Quick Score

## Проблема

Когда пользователь пишет `/start` боту, бот не отвечает. Это происходит потому, что бот не запущен в режиме обработки команд.

## Решение

Запустите обработчик команд бота, который будет:
- ✅ Отвечать на команду `/start`
- ✅ Отправлять пользователям их Chat ID
- ✅ Обрабатывать другие команды (`/id`, `/help`)

---

## 🚀 Запуск бота (локально)

### 1. Откройте новый терминал

**Важно:** НЕ останавливайте Flask приложение! Запустите бота в **отдельном терминале**.

### 2. Перейдите в директорию проекта

```bash
cd C:\Cursor\Tournaments_v.1
```

### 3. Запустите обработчик бота

```bash
python telegram_bot_handler.py
```

### 4. Вы должны увидеть:

```
============================================================
🤖 Quick Score Telegram Bot Handler
============================================================

2025-10-28 22:10:00 - INFO - 🤖 Бот Quick Score запущен и готов к работе!
2025-10-28 22:10:00 - INFO - Ожидание команд от пользователей...
```

---

## 📱 Как проверить работу бота

### Шаг 1: Найдите бота в Telegram

Откройте Telegram и найдите вашего бота (уточните username у организатора).

### Шаг 2: Напишите `/start`

Бот должен ответить:

```
🎉 Привет, Andrey!

Добро пожаловать в бот Quick Score! 

Этот бот будет отправлять вам уведомления о:
✅ Одобрении заявки на участие в турнире
📅 Расписании ваших матчей
🏆 Результатах турниров

📋 Ваши данные для регистрации:

🔹 Username: @andvm
🔹 Chat ID: 123456789

💡 Как получать уведомления:

1. При подаче заявки на турнир укажите в поле "Telegram":
   • Ваш Username: @andvm
   • ИЛИ ваш Chat ID: 123456789

2. После одобрения вашей заявки вы автоматически получите уведомление сюда!

📞 Команды бота:
/start - Показать это сообщение
/id - Получить ваш Chat ID
/help - Помощь по использованию

Удачи в турнирах! 🏆
```

### Шаг 3: Получите ваш Chat ID

Напишите `/id` и бот отправит ваш Chat ID:

```
🆔 Ваш Chat ID: 123456789

Скопируйте это число и укажите его в поле "Telegram" 
при подаче заявки на турнир.

🔹 Ваш Username: @andvm

💡 Совет: Chat ID надёжнее, чем username, так как 
username может быть изменён.
```

---

## 🖥️ Запуск бота на VDS сервере

### Вариант 1: Запуск в фоновом режиме (screen)

```bash
# Установите screen если его нет
sudo apt install screen

# Создайте новую сессию screen
screen -S telegram_bot

# Перейдите в директорию проекта
cd /home/deploy/quick-score

# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите бота
python telegram_bot_handler.py

# Нажмите Ctrl+A, затем D для выхода из screen (бот продолжит работу)
```

Чтобы вернуться к боту:
```bash
screen -r telegram_bot
```

### Вариант 2: Создание systemd сервиса (рекомендуется)

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

Добавьте содержимое:

```ini
[Unit]
Description=Quick Score Telegram Bot Handler
After=network.target

[Service]
Type=simple
User=deploy
WorkingDirectory=/home/deploy/quick-score
Environment="PATH=/home/deploy/quick-score/venv/bin"
ExecStart=/home/deploy/quick-score/venv/bin/python telegram_bot_handler.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
# Перезагрузите конфигурацию systemd
sudo systemctl daemon-reload

# Запустите бота
sudo systemctl start telegram-bot

# Включите автозапуск при загрузке системы
sudo systemctl enable telegram-bot

# Проверьте статус
sudo systemctl status telegram-bot

# Посмотрите логи
sudo journalctl -u telegram-bot -f
```

---

## 🔍 Проверка работы

### Проверка логов (локально)

При запуске `telegram_bot_handler.py` вы будете видеть логи в терминале:

```
2025-10-28 22:10:00 - INFO - Получена команда от Andrey (@andvm, chat_id=123456789): /start
2025-10-28 22:10:00 - INFO - Получена команда от Ivan (@ivan_petrov, chat_id=987654321): /id
```

### Проверка логов (на VDS)

```bash
# Если используете screen
screen -r telegram_bot

# Если используете systemd
sudo journalctl -u telegram-bot -n 50 --no-pager

# Следить за логами в реальном времени
sudo journalctl -u telegram-bot -f
```

---

## ❓ Часто задаваемые вопросы

### Бот не отвечает на команды

**Проблема:** Обработчик бота не запущен.

**Решение:** 
1. Убедитесь, что `telegram_bot_handler.py` запущен
2. Проверьте логи на наличие ошибок
3. Убедитесь, что `TELEGRAM_BOT_TOKEN` правильно настроен в `config.py`

### Бот работает, но уведомления не приходят

**Проблема:** Это две разные функции!
- `telegram_bot_handler.py` - обрабатывает команды от пользователей
- Flask приложение - отправляет уведомления

**Решение:** Оба должны работать одновременно:
1. Flask приложение: `python app.py`
2. Telegram бот: `python telegram_bot_handler.py`

### Как остановить бота?

**Локально:**
```
Ctrl+C в терминале где запущен бот
```

**На VDS (screen):**
```bash
screen -r telegram_bot
# Нажмите Ctrl+C
```

**На VDS (systemd):**
```bash
sudo systemctl stop telegram-bot
```

---

## 🎯 Итого

После запуска `telegram_bot_handler.py`:

1. ✅ Пользователь пишет `/start` → получает приветствие и свой Chat ID
2. ✅ Пользователь пишет `/id` → получает свой Chat ID
3. ✅ Пользователь указывает Chat ID при подаче заявки
4. ✅ Админ одобряет заявку → пользователь получает уведомление

**Важно:** Для работы уведомлений нужны ОБА процесса:
- `python app.py` - Flask приложение (отправка уведомлений)
- `python telegram_bot_handler.py` - Telegram бот (обработка команд)

---

**Удачи! 🚀**




