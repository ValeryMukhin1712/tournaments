# Настройка Telegram Bot для обратной связи

Эта инструкция поможет вам настроить Telegram бота для получения сообщений обратной связи от пользователей вашего приложения Quick Score.

## Шаг 1: Создание Telegram бота

1. **Откройте Telegram** и найдите бота **@BotFather**
2. **Отправьте команду** `/newbot`
3. **Введите имя бота** (например, `Quick Score Feedback Bot`)
4. **Введите username бота** (должен заканчиваться на `bot`, например: `quickscore_feedback_bot`)
5. **Скопируйте токен бота** - BotFather отправит вам сообщение с токеном вида:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890
   ```
   ⚠️ **ВАЖНО**: Сохраните этот токен - это `TELEGRAM_BOT_TOKEN`

## Шаг 2: Получение Chat ID

### Вариант 1: Через специального бота

1. Найдите в Telegram бота **@userinfobot** или **@getmyid_bot**
2. Отправьте любое сообщение боту
3. Бот ответит вам с вашим **Chat ID** (число, например: `123456789`)

### Вариант 2: Через вашего бота

1. **Напишите любое сообщение** вашему новому боту (тому, которого вы только что создали)
2. **Откройте в браузере** следующий URL (замените `YOUR_BOT_TOKEN` на ваш токен):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
3. **Найдите в ответе** значение `"chat":{"id": XXXXXX}` - это и есть ваш **Chat ID**

Пример ответа:
```json
{
  "ok": true,
  "result": [{
    "update_id": 123456789,
    "message": {
      "message_id": 1,
      "from": {"id": 987654321, ...},
      "chat": {"id": 987654321, "type": "private"},  // <-- Это ваш Chat ID
      "text": "Привет"
    }
  }]
}
```

## Шаг 3: Настройка переменных окружения

### Для локальной разработки:

Создайте файл `.env` в корне проекта (если его нет) и добавьте:

```bash
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890
TELEGRAM_CHAT_ID=987654321
```

### Для Railway (production):

1. Откройте ваш проект на **Railway.app**
2. Перейдите в раздел **Variables**
3. Добавьте две новые переменные:
   - **Имя**: `TELEGRAM_BOT_TOKEN`  
     **Значение**: `ваш_токен_бота`
   - **Имя**: `TELEGRAM_CHAT_ID`  
     **Значение**: `ваш_chat_id`
4. Сохраните изменения
5. Railway автоматически перезапустит приложение

## Шаг 4: Проверка работы

1. **Перезапустите приложение** (если используете локально)
2. Откройте главную страницу Quick Score
3. Нажмите кнопку **"Связаться с автором"** внизу страницы
4. Заполните и отправьте тестовое сообщение
5. **Проверьте Telegram** - вы должны получить красиво отформатированное сообщение от вашего бота!

## Пример получаемого сообщения

```
🔔 Новое сообщение из Quick Score

👤 Имя: Иван Петров
📧 Email: ivan@example.com
✈️ Telegram: @ivan_petrov
📝 Тема: Вопрос

💬 Сообщение:
Отличное приложение! Можно ли добавить поддержку тенниса?

⏰ Время: 28.10.2025 16:45:30
```

💡 **Удобство:** Если пользователь указал свой Telegram, вы сможете связаться с ним напрямую для быстрого ответа!

## Безопасность

⚠️ **ВАЖНО**: 
- **Никогда не публикуйте** токен бота в открытом доступе (GitHub, форумы и т.д.)
- **Не добавляйте** файл `.env` в git (он должен быть в `.gitignore`)
- Если токен случайно попал в публичный репозиторий - **немедленно создайте нового бота** через @BotFather

## Решение проблем

### Сообщения не приходят

1. Проверьте, что переменные окружения установлены правильно
2. Убедитесь, что вы отправили хотя бы одно сообщение боту перед использованием
3. Проверьте логи приложения на наличие ошибок

### Ошибка "Bot token not found"

- Проверьте правильность написания переменной `TELEGRAM_BOT_TOKEN`
- Убедитесь, что токен скопирован полностью, без лишних пробелов

### Ошибка "Chat not found"

- Убедитесь, что вы написали боту хотя бы одно сообщение
- Проверьте правильность `TELEGRAM_CHAT_ID`

---

**Готово!** Теперь вы будете получать все сообщения от пользователей прямо в Telegram! 🎉

