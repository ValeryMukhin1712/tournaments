# 🚀 Настройка двух репозиториев для VDS серверов

## 📋 Цели настройки

- **Сервер 1 (Prod)**: `tournament-admin@45.135.164.202` → репозиторий `tournaments-prod`
- **Сервер 2 (Dev)**: `deploy@89.19.44.212` → репозиторий `tournaments-dev`
- Разделение сред разработки и продакшена
- Возможность тестирования на dev перед развертыванием на prod

## 🛠️ Шаг 1: Создание репозиториев

### На GitHub:
1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Создайте два репозитория:
   - `tournaments-prod` (для продакшена)
   - `tournaments-dev` (для разработки)

### На GitLab (альтернатива):
1. Перейдите на https://gitlab.com
2. Создайте группу или используйте личный аккаунт
3. Создайте два проекта

## 🔑 Шаг 2: Настройка SSH ключей

### Генерация SSH ключа (если нет):
```bash
# На локальной машине
ssh-keygen -t ed25519 -C "your_email@example.com" -f ~/.ssh/id_ed25519_tournaments
```

### Добавление ключа на GitHub:
1. Скопируйте публичный ключ: `cat ~/.ssh/id_ed25519_tournaments.pub`
2. На GitHub: Settings → SSH and GPG keys → New SSH key
3. Вставьте ключ

### Настройка SSH конфига:
```bash
# В ~/.ssh/config добавьте:
Host github-prod
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_tournaments

Host github-dev
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_tournaments
```

## 📤 Шаг 3: Загрузка кода в репозитории

### Использование существующего репозитория quick-score

Поскольку у вас уже есть репозиторий `https://github.com/ValeryMukhin1712/quick-score`, мы будем использовать его как источник.

#### Вариант 1: Копирование через локальный клон (рекомендуется)

```bash
# Перейдите в директорию проекта
cd "C:\Cursor\Tournaments_v.1 — 04598ed коммит в main_1"

# Добавьте remote для существующего репозитория
git remote add quickscore https://github.com/ValeryMukhin1712/quick-score.git

# Получите содержимое из quick-score
git pull quickscore main --allow-unrelated-histories

# Разрешите конфликты слияния если они возникнут
# После разрешения выполните:
git add .
git commit -m "Merge content from quick-score repository"

# Теперь настройте remotes для двух серверов
git remote add prod https://github.com/ValeryMukhin1712/tournaments.git
git remote add dev https://github.com/ValeryMukhin1712/tournaments-dev.git

# Отправьте код в оба репозитория
git push prod main
git checkout -b develop
git push dev develop
```

#### Вариант 2: Использование скрипта copy_repo_content.sh

```bash
# Запустите скрипт копирования
chmod +x copy_repo_content.sh
./copy_repo_content.sh

# Следуйте инструкциям скрипта
```

### Проверка содержимого репозиториев

После копирования проверьте, что в обоих репозиториях:
- `https://github.com/ValeryMukhin1712/tournaments` (prod)
- `https://github.com/ValeryMukhin1712/tournaments-dev` (dev)

Присутствуют все необходимые файлы из quick-score.

## 🖥️ Шаг 4: Настройка серверов

### Сервер 1 (Prod - tournament-admin@45.135.164.202):

```bash
# Подключитесь к серверу
ssh tournament-admin@45.135.164.202

# Перейдите в директорию проекта
cd /home/tournament-admin/quick-score

# Если директория не пустая - сделайте backup
tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz .

# Клонируйте продакшн репозиторий
git clone git@github-prod:YOUR_USERNAME/tournaments-prod.git .

# Или если директория не пустая:
git remote set-url origin git@github-prod:YOUR_USERNAME/tournaments-prod.git
git pull origin main
```

### Сервер 2 (Dev - deploy@89.19.44.212):

```bash
# Подключитесь к серверу
ssh deploy@89.19.44.212

# Перейдите в директорию проекта
cd /home/deploy/quick-score

# Аналогично настройте для dev репозитория
git clone git@github-dev:YOUR_USERNAME/tournaments-dev.git .
# или
git remote set-url origin git@github-dev:YOUR_USERNAME/tournaments-dev.git
git pull origin develop
```

## 🔄 Шаг 5: Workflow работы с репозиториями

### Локальная разработка:
```bash
# Работаете в ветке develop
git checkout develop

# Вносите изменения
# ... редактирование кода ...

# Коммитите изменения
git add .
git commit -m "New feature: description"

# Отправляете в dev репозиторий
git push dev develop
```

### Развертывание на dev сервере:
```bash
# На dev сервере
ssh deploy@89.19.44.212
cd /home/deploy/quick-score

# Останавливаете приложение
sudo supervisorctl stop tournaments

# Обновляете код
git pull origin develop

# Применяете миграции если нужно
python3 server_migration.py

# Запускаете приложение
sudo supervisorctl start tournaments
```

### Развертывание на prod сервере:
```bash
# Сначала тестируете на dev
# После успешного тестирования:

# Локально сливаете develop в main
git checkout main
git merge develop
git push prod main

# На prod сервере
ssh tournament-admin@45.135.164.202
cd /home/tournament-admin/quick-score

# Останавливаете приложение
sudo supervisorctl stop tournaments

# Обновляете код
git pull origin main

# Применяете миграции если нужно
python3 server_migration.py

# Запускаете приложение
sudo supervisorctl start tournaments
```

## 🔐 Шаг 6: Безопасность

### На серверах настройте SSH ключи:
```bash
# На каждом сервере сгенерируйте SSH ключ
ssh-keygen -t ed25519 -C "server@yourdomain.com"

# Добавьте публичный ключ на GitHub в deploy keys для каждого репозитория
# GitHub: Repository Settings → Deploy keys → Add deploy key
```

## 📊 Мониторинг и логи

### Проверка статуса репозиториев:
```bash
# На каждом сервере
git status
git log --oneline -5
git remote -v
```

## 🚨 Важные замечания

1. **Никогда не коммитите secrets** (пароли, API ключи) в репозиторий
2. **Используйте .gitignore** для исключения чувствительных файлов
3. **Делайте backup** перед обновлениями
4. **Тестируйте на dev** перед продакшеном
5. **Мониторьте логи** после обновлений

## 🔄 Синхронизация между репозиториями

Если нужно синхронизировать изменения между репозиториями:

```bash
# Добавить второй remote
git remote add prod git@github-prod:YOUR_USERNAME/tournaments-prod.git
git remote add dev git@github-dev:YOUR_USERNAME/tournaments-dev.git

# Синхронизировать из dev в prod
git checkout main
git pull dev develop
git push prod main
```

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте SSH ключи: `ssh -T git@github.com`
2. Проверьте права доступа к репозиториям
3. Проверьте логи Git: `git log --oneline`
4. Проверьте статус: `git status`
