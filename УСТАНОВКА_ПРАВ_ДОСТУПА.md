# 🔐 Установка прав доступа на файл базы данных

## ⚠️ ВАЖНО: Где выполнять команды

**Команды `chmod` и `chown` выполняются НА СЕРВЕРЕ (VDS), а не на локальном компьютере!**

Эти команды работают только в Linux/Unix системах. На Windows они не работают.

## 📍 Где выполнять

1. **Подключитесь к серверу через SSH** (с локального компьютера):
   ```bash
   ssh deploy@ваш_сервер
   ```

2. **Перейдите в директорию проекта** (на сервере):
   ```bash
   cd ~/quick-score
   ```
   или полный путь:
   ```bash
   cd /home/deploy/quick-score
   ```

3. **Выполните команды установки прав** (на сервере):
   ```bash
   chmod 644 instance/tournament.db
   chown deploy:deploy instance/tournament.db
   ```

## 📋 Полная последовательность действий

### Пример: После копирования БД на сервер

```bash
# 1. На локальном компьютере - скопируйте БД на сервер
scp instance/tournament.db deploy@ваш_сервер:/home/deploy/quick-score/instance/tournament.db

# 2. Подключитесь к серверу
ssh deploy@ваш_сервер

# 3. На сервере - перейдите в директорию проекта
cd ~/quick-score

# 4. На сервере - установите права доступа
chmod 644 instance/tournament.db
chown deploy:deploy instance/tournament.db

# 5. На сервере - проверьте права (опционально)
ls -lh instance/tournament.db
# Должно показать: -rw-r--r-- 1 deploy deploy ... tournament.db

# 6. На сервере - перезапустите приложение
sudo systemctl restart tournaments
```

## 🔍 Пояснение команд

### `chmod 644`
- Устанавливает права доступа к файлу
- `6` (rw-) - владелец может читать и писать
- `4` (r--) - группа может только читать
- `4` (r--) - остальные могут только читать
- Итого: `-rw-r--r--`

### `chown deploy:deploy`
- Устанавливает владельца и группу файла
- Первый `deploy` - имя пользователя-владельца
- Второй `deploy` - имя группы
- Файл будет принадлежать пользователю `deploy` и группе `deploy`

### `instance/tournament.db`
- Путь к файлу БД относительно текущей директории
- Если вы в `~/quick-score`, то полный путь: `/home/deploy/quick-score/instance/tournament.db`

## ✅ Проверка прав

После установки прав проверьте результат:

```bash
ls -lh instance/tournament.db
```

Ожидаемый вывод:
```
-rw-r--r-- 1 deploy deploy 1234567 Dec 15 10:30 instance/tournament.db
```

Где:
- `-rw-r--r--` - права доступа (644)
- `deploy` - владелец
- `deploy` - группа
- `1234567` - размер файла в байтах

## ❌ Частые ошибки

### Ошибка: "chmod: command not found"
**Причина:** Вы пытаетесь выполнить команду на Windows  
**Решение:** Выполните команду на сервере через SSH

### Ошибка: "Permission denied"
**Причина:** Недостаточно прав для изменения файла  
**Решение:** Используйте `sudo`:
```bash
sudo chmod 644 instance/tournament.db
sudo chown deploy:deploy instance/tournament.db
```

### Ошибка: "No such file or directory"
**Причина:** Неправильный путь к файлу  
**Решение:** Проверьте текущую директорию:
```bash
pwd  # Покажет текущую директорию
ls -la instance/  # Покажет содержимое папки instance
```

## 📝 Когда нужно устанавливать права

Права нужно устанавливать:
- ✅ После копирования БД на сервер
- ✅ После создания новой БД на сервере
- ✅ После восстановления БД из бэкапа
- ✅ Если приложение не может получить доступ к БД

## 🔗 Связанные документы

- `database_logic_and_migration.md` - общая логика работы с БД
- `transfer_database_to_vds.md` - инструкции по переносу БД

