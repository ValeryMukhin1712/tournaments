# 🎯 Алгоритм определения места подачи в зависимости от счёта

## 📋 Общая концепция

В бадминтоне место подачи определяется по правилам:
- **Подача переходит к другой команде**, когда команда, которая принимала, выигрывает очко
- **Подача остаётся у той же команды**, когда команда, которая подавала, выигрывает очко
- **Позиция подающего игрока** (верх/низ) меняется при каждом выигранном очке своей командой

## 🔢 Основные переменные

- `scoreLeft` - счёт левой команды (отображается слева на экране)
- `scoreRight` - счёт правой команды (отображается справа на экране)
- `shuttlecockPosition` - текущая позиция индикатора подачи:
  - `'top_left'` - верхний левый угол
  - `'bottom_left'` - нижний левый угол
  - `'top_right'` - верхний правый угол
  - `'bottom_right'` - нижний правый угол
- `swap_count` - счётчик смен половин корта (влияет на интерпретацию счёта)

## 🎲 Шаг 1: Определение команды, которая должна подавать

### Основной принцип

Команда, которая подаёт, определяется по **текущей позиции индикатора** `shuttlecockPosition`, а не по общему счёту.

```javascript
const isServingLeft = shuttlecockPosition === 'top_left' || shuttlecockPosition === 'bottom_left';
const isServingRight = shuttlecockPosition === 'top_right' || shuttlecockPosition === 'bottom_right';
```

**Правило:**
- Если `shuttlecockPosition` находится на **левой стороне** (`top_left` или `bottom_left`) → подаёт **левая команда**
- Если `shuttlecockPosition` находится на **правой стороне** (`top_right` или `bottom_right`) → подаёт **правая команда**

**Важно:** Позиция индикатора является источником истины о том, кто подаёт. При каждом забитом очке позиция обновляется согласно правилам бадминтона.

## 🎯 Шаг 2: Определение позиции подающего игрока (верх/низ)

Позиция подающего игрока (верх или низ) определяется по **счёту его команды**:

### Для правой команды:

```javascript
if (scoreRight % 2 === 0) {
    shuttlecockPosition = 'top_right';    // Чётное → верхний правый
} else {
    shuttlecockPosition = 'bottom_right'; // Нечётное → нижний правый
}
```

**Правило:**
- Если **счёт правой команды чётный** → подаёт из **верхнего правого угла** (`top_right`)
- Если **счёт правой команды нечётный** → подаёт из **нижнего правого угла** (`bottom_right`)

**Примеры:**
- Счёт правой команды 0 → чётное → `top_right`
- Счёт правой команды 1 → нечётное → `bottom_right`
- Счёт правой команды 2 → чётное → `top_right`
- Счёт правой команды 3 → нечётное → `bottom_right`

### Для левой команды:

```javascript
if (scoreLeft % 2 === 0) {
    shuttlecockPosition = 'bottom_left'; // Чётное → нижний левый
} else {
    shuttlecockPosition = 'top_left';    // Нечётное → верхний левый
}
```

**Правило:**
- Если **счёт левой команды чётный** → подаёт из **нижнего левого угла** (`bottom_left`)
- Если **счёт левой команды нечётный** → подаёт из **верхнего левого угла** (`top_left`)

**Примеры:**
- Счёт левой команды 0 → чётное → `bottom_left`
- Счёт левой команды 1 → нечётное → `top_left`
- Счёт левой команды 2 → чётное → `bottom_left`
- Счёт левой команды 3 → нечётное → `top_left`

### Примечание о функции `calculateShuttlecockPositionFromScore()`

Эта функция используется только для **пересчёта позиции на основе счёта** (например, при загрузке игры из базы данных). В реальной игре позиция обновляется через функцию `handlePointScored()`, которая использует текущую позицию `shuttlecockPosition` для определения команды, которая подаёт.

## 🔄 Шаг 3: Обновление позиции после забитого очка

### Функция `handlePointScored(scoringTeam)`

Когда забивается очко, позиция обновляется по следующим правилам:

#### Случай 1: Подающая команда выиграла очко

**Левая команда подавала и выиграла:**
```javascript
if (shuttlecockPosition === 'top_left') {
    shuttlecockPosition = 'bottom_left';  // Переход: верх → низ
} else {
    shuttlecockPosition = 'top_left';     // Переход: низ → верх
}
```

**Правая команда подавала и выиграла:**
```javascript
if (shuttlecockPosition === 'top_right') {
    shuttlecockPosition = 'bottom_right'; // Переход: верх → низ
} else {
    shuttlecockPosition = 'top_right';    // Переход: низ → верх
}
```

**Логика:** Позиция остаётся на той же стороне, но переключается между верхом и низом.

#### Случай 2: Принимающая команда выиграла очко

**Левая команда выиграла (правая подавала):**
```javascript
if (scoreLeft % 2 === 0) {
    shuttlecockPosition = 'bottom_left'; // Чётное → нижний левый
} else {
    shuttlecockPosition = 'top_left';   // Нечётное → верхний левый
}
```

**Правая команда выиграла (левая подавала):**
```javascript
if (scoreRight % 2 === 0) {
    shuttlecockPosition = 'top_right';    // Чётное → верхний правый
} else {
    shuttlecockPosition = 'bottom_right'; // Нечётное → нижний правый
}
```

**Логика:** Подача переходит на другую сторону, позиция определяется по новому счёту выигравшей команды.

## 📊 Полный пример последовательности

### Начало игры (0:0)

**Исходное состояние:** `shuttlecockPosition = 'bottom_left'` (по умолчанию)

1. **Текущая позиция:** `bottom_left` → подаёт **левая команда**
2. **scoreLeft = 0** (чётное) → позиция должна быть `bottom_left` ✅
3. **Результат:** `shuttlecockPosition = 'bottom_left'`

### Левая команда выигрывает очко (1:0)

1. **Текущая позиция:** `bottom_left` → подавала **левая команда**
2. **Левая команда выиграла** → позиция остаётся на левой стороне, переключается верх ↔ низ
3. **Было:** `bottom_left` → **Стало:** `top_left`
4. **Результат:** `shuttlecockPosition = 'top_left'`

### Правая команда выигрывает очко (1:1)

1. **Текущая позиция:** `top_left` → подавала **левая команда**
2. **Правая команда выиграла** → подача переходит на правую сторону
3. **scoreRight = 1** (нечётное) → позиция `bottom_right`
4. **Результат:** `shuttlecockPosition = 'bottom_right'`

### Правая команда выигрывает очко (1:2)

1. **Текущая позиция:** `bottom_right` → подавала **правая команда**
2. **Правая команда выиграла** → позиция остаётся на правой стороне, переключается верх ↔ низ
3. **Было:** `bottom_right` → **Стало:** `top_right`
4. **Результат:** `shuttlecockPosition = 'top_right'`

### Левая команда выигрывает очко (2:2)

1. **Текущая позиция:** `top_right` → подавала **правая команда**
2. **Левая команда выиграла** → подача переходит на левую сторону
3. **scoreLeft = 2** (чётное) → позиция `bottom_left`
4. **Результат:** `shuttlecockPosition = 'bottom_left'`

## 🔀 Влияние swap_count на интерпретацию счёта

Переменная `swap_count` используется для правильной интерпретации счёта при сохранении в базу данных и отображении в журнале.

### Правило интерпретации:

```javascript
if (swap_count % 2 == 0) {
    // Чётное количество смен:
    // левый счёт → participant1 (синий)
    // правый счёт → participant2 (красный)
    blueTeamScore = scoreLeft;
    redTeamScore = scoreRight;
} else {
    // Нечётное количество смен:
    // правый счёт → participant1 (синий)
    // левый счёт → participant2 (красный)
    blueTeamScore = scoreRight;
    redTeamScore = scoreLeft;
}
```

**Когда swap_count увеличивается:**
- При смене половин корта (кнопка "Смена половин")
- При переходе в режим "Просмотр" и выходе из него

## 🎨 Определение направления стрелки в журнале

### Функция `getArrowDirection(serverName, score, result)`

Для отображения стрелки в журнале используется формула:

```javascript
// Для синей команды (participant1):
isEven = (leftScore + resultValue) % 2 === 0;

// Для красной команды (participant2):
isEven = (rightScore + resultValue) % 2 === 0;
```

Где:
- `leftScore` / `rightScore` - счёт команды **ДО** розыгрыша
- `resultValue` - результат розыгрыша (1 = выиграл, 0 = проиграл)

**Правило:**
- Если `isEven === true` → стрелка `↗` (розовая, вверх-вправо)
- Если `isEven === false` → стрелка `↘` (голубая, вниз-вправо)

**Пример:**
- Счёт синей команды ДО розыгрыша: 2
- Результат: выиграл (1)
- `(2 + 1) % 2 = 3 % 2 = 1` (нечётное) → стрелка `↘`

## 📝 Итоговая формула

### Для определения команды, которая подаёт:

```
Команда определяется по текущей позиции shuttlecockPosition:
- Если позиция на левой стороне (top_left или bottom_left) → подаёт левая команда
- Если позиция на правой стороне (top_right или bottom_right) → подаёт правая команда
```

### Для определения позиции подающего игрока (верх/низ):

```
Если подаёт правая команда:
   Если scoreRight % 2 === 0 → top_right
   Иначе → bottom_right

Если подаёт левая команда:
   Если scoreLeft % 2 === 0 → bottom_left
   Иначе → top_left
```

### Для обновления позиции после забитого очка:

```
1. Определить команду, которая подавала (по текущей позиции shuttlecockPosition)
2. Если подающая команда выиграла:
      Позиция остаётся на той же стороне, переключается верх ↔ низ
3. Если принимающая команда выиграла:
      Подача переходит на другую сторону
      Позиция определяется по счёту выигравшей команды:
      - Левая команда: чётный счёт → bottom_left, нечётный → top_left
      - Правая команда: чётный счёт → top_right, нечётный → bottom_right
```

### Для определения стрелки в журнале:

```
1. Вычислить счёт ДО розыгрыша (scoreBefore)
2. Определить команду подающего (participant1 или participant2)
3. Если participant1:
      isEven = (leftScore + result) % 2 === 0
   Иначе:
      isEven = (rightScore + result) % 2 === 0
4. Если isEven → ↗, иначе → ↘
```

## ✅ Проверка корректности

Алгоритм соответствует правилам бадминтона:
- ✅ Команда, которая подаёт, определяется по текущей позиции индикатора
- ✅ Подача переходит к другой команде при проигрыше подающей команды
- ✅ Позиция подающего меняется при каждом выигранном очке своей командой
- ✅ Позиция определяется чётностью счёта команды
- ✅ При переходе подачи на другую сторону позиция определяется по счёту выигравшей команды

