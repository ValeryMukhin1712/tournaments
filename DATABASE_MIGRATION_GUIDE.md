# 📦 Инструкция по переносу локальной базы данных на сервер

## 📋 Подготовка

### 1. Определение файла базы данных

Локально база данных может находиться в двух местах:
- `tournament.db` (в корне проекта) - используется по умолчанию в `app.py`
- `instance/tournament.db` - используется в `config.py`

**Проверьте, какой файл используется:**
```powershell
# Проверка размера и даты изменения
Get-ChildItem -Path "tournament.db","instance\tournament.db" -ErrorAction SilentlyContinue | Select-Object FullName,Length,LastWriteTime
```

**Рекомендуется использовать более свежий файл** (с более поздней датой изменения).

---

## 🛑 Шаг 1: Остановка приложения на сервере

### Подключение к серверу
```bash
ssh deploy@89.19.44.212
```

### Остановка сервиса Production
```bash
sudo systemctl stop tournaments
```

### Проверка остановки
```bash
# Проверка статуса (должен быть inactive)
sudo systemctl status tournaments

# Проверка, что порт 5000 свободен
ss -tuln | grep 5000
```

### Остановка сервиса Dev (если нужно)
```bash
sudo systemctl stop tournaments-dev
```

---

## 📤 Шаг 2: Копирование базы данных через WinSCP

### 2.1 Подготовка локального файла

**Важно:** Перед копированием убедитесь, что:
- ✅ Локальное приложение остановлено (если запущено)
- ✅ Файл базы данных не заблокирован другими процессами
- ✅ Выбран правильный файл (более свежий)

### 2.2 Подключение через WinSCP

1. **Откройте WinSCP**
2. **Создайте новое соединение:**
   - **Протокол:** SFTP
   - **Имя хоста:** `89.19.44.212`
   - **Порт:** `22`
   - **Имя пользователя:** `deploy`
   - **Пароль:** (ваш пароль)

3. **Подключитесь к серверу**

### 2.3 Копирование файла

**Локальный путь (источник):**
```
C:\Cursor\Tournaments_v.1 — 04598ed коммит в main_1\tournament.db
```
или
```
C:\Cursor\Tournaments_v.1 — 04598ed коммит в main_1\instance\tournament.db
```

**Удаленный путь (назначение):**
```
/home/deploy/quick-score/instance/tournament.db
```

**Действия:**
1. В WinSCP перейдите в локальную директорию проекта
2. Найдите файл `tournament.db` или `instance/tournament.db`
3. Перетащите файл в удаленную директорию `/home/deploy/quick-score/instance/`
4. **Важно:** При замене существующего файла WinSCP спросит о подтверждении - нажмите "Да"

### 2.4 Проверка прав доступа на сервере

После копирования проверьте права доступа:

```bash
# Подключитесь к серверу через SSH
ssh deploy@89.19.44.212

# Перейдите в директорию проекта
cd /home/deploy/quick-score

# Проверьте права доступа
ls -la instance/tournament.db

# Установите правильные права (если нужно)
chmod 644 instance/tournament.db
chown deploy:deploy instance/tournament.db
```

---

## 🚀 Шаг 3: Запуск приложения на сервере

### 3.1 Запуск сервиса Production

```bash
# Запуск сервиса
sudo systemctl start tournaments

# Проверка статуса
sudo systemctl status tournaments

# Проверка логов (первые 50 строк)
sudo journalctl -u tournaments -n 50 --no-pager
```

### 3.2 Проверка работы

```bash
# Проверка, что порт 5000 слушается
ss -tuln | grep 5000

# Проверка доступности через curl
curl -I https://quickscore.sytes.net/

# Мониторинг логов в реальном времени (Ctrl+C для выхода)
sudo journalctl -u tournaments -f
```

### 3.3 Запуск сервиса Dev (если нужно)

```bash
sudo systemctl start tournaments-dev
sudo systemctl status tournaments-dev
```

---

## ✅ Проверочный список

- [ ] Локальное приложение остановлено (если было запущено)
- [ ] Определен правильный файл базы данных для копирования
- [ ] Подключение к серверу через WinSCP установлено
- [ ] Файл базы данных скопирован в `/home/deploy/quick-score/instance/tournament.db`
- [ ] Права доступа на файл установлены корректно
- [ ] Сервис `tournaments` остановлен на сервере
- [ ] Сервис `tournaments` запущен на сервере
- [ ] Статус сервиса показывает `active (running)`
- [ ] Сайт доступен по адресу `https://quickscore.sytes.net/`
- [ ] В логах нет критических ошибок

---

## 🔄 Альтернативный способ: Резервное копирование перед заменой

Если хотите сохранить текущую базу данных на сервере перед заменой:

```bash
# На сервере, перед копированием нового файла
cd /home/deploy/quick-score/instance
cp tournament.db tournament.db.backup_$(date +%Y%m%d_%H%M%S)
```

---

## 🚨 Восстановление в случае проблем

Если что-то пошло не так:

```bash
# Остановите сервис
sudo systemctl stop tournaments

# Восстановите резервную копию (если создавали)
cd /home/deploy/quick-score/instance
cp tournament.db.backup_YYYYMMDD_HHMMSS tournament.db

# Или вернитесь к предыдущей версии
# (если у вас есть git-репозиторий с историей)

# Запустите сервис
sudo systemctl start tournaments
```

---

## 📝 Дополнительная информация

### Расположение файлов на сервере

- **Production:** `/home/deploy/quick-score/instance/tournament.db`
- **Development:** `/home/deploy/quick-score-dev/instance/tournament.db`

### Управление сервисами

```bash
# Статус
sudo systemctl status tournaments

# Перезапуск
sudo systemctl restart tournaments

# Остановка
sudo systemctl stop tournaments

# Запуск
sudo systemctl start tournaments

# Логи
sudo journalctl -u tournaments -f
```

---

## ⚠️ Важные замечания

1. **Всегда делайте резервную копию** базы данных на сервере перед заменой
2. **Останавливайте приложение** перед копированием, чтобы избежать повреждения базы данных
3. **Проверяйте права доступа** после копирования файла
4. **Проверяйте логи** после запуска, чтобы убедиться, что нет ошибок
5. **Тестируйте на Dev окружении** перед переносом в Production (если возможно)



