# ⚡ Quick Start Guide

Быстрая установка Quick Score Tournaments на Ubuntu VDS за 5 минут.

## 📋 Предварительные требования

- Ubuntu 22.04 LTS VDS
- Root доступ через SSH
- Домен (опционально)

---

## 🚀 Установка (3 команды)

### 1. Клонировать репозиторий

```bash
cd /root
git clone https://github.com/ValeryMukhin1712/quick-score.git
cd quick-score/deployment
```

### 2. Настроить сервер

```bash
chmod +x scripts/*.sh
sudo ./scripts/setup_server.sh
```

⏱️ **Время выполнения:** ~5-10 минут

**Что происходит:**
- Обновление системы
- Установка Python, Nginx, Git
- Создание пользователя `deploy`
- Настройка SSH, Firewall, Fail2Ban
- Настройка автообновлений

### 3. Установить SSH ключ

**На вашем локальном компьютере:**

```bash
ssh-copy-id -i ~/.ssh/id_rsa.pub deploy@YOUR_SERVER_IP
```

Если у вас еще нет SSH ключа:

```bash
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

**Проверить подключение:**

```bash
ssh deploy@YOUR_SERVER_IP
```

### 4. Деплой приложения

**На сервере (от пользователя deploy):**

```bash
cd /home/deploy
git clone https://github.com/ValeryMukhin1712/quick-score.git app
cd app/deployment
./scripts/deploy_app.sh
```

⏱️ **Время выполнения:** ~3-5 минут

**Что происходит:**
- Создание виртуального окружения Python
- Установка зависимостей
- Настройка Gunicorn
- Настройка Nginx
- Запуск приложения

---

## ✅ Проверка

Откройте в браузере:

```
http://YOUR_SERVER_IP
```

Вы должны увидеть главную страницу Quick Score!

---

## 🔒 SSL сертификат (опционально)

Если у вас есть домен:

```bash
# Замените yourdomain.com на ваш домен
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

Certbot автоматически:
- Получит SSL сертификат
- Настроит Nginx для HTTPS
- Настроит автообновление сертификата

---

## 🔄 Автоматический деплой (опционально)

Настройка автоматического обновления при push в GitHub:

```bash
cd /home/deploy/app/deployment
./scripts/setup_webhook.sh
```

Следуйте инструкциям для настройки webhook в GitHub.

---

## 📊 Полезные команды

### Управление приложением

```bash
# Статус
sudo systemctl status tournaments

# Перезапуск
sudo systemctl restart tournaments

# Остановка
sudo systemctl stop tournaments

# Запуск
sudo systemctl start tournaments

# Логи в реальном времени
sudo journalctl -u tournaments -f
```

### Обновление приложения

```bash
cd /home/deploy/app/deployment
./scripts/deploy_app.sh --update
```

### Проверка Nginx

```bash
# Тест конфигурации
sudo nginx -t

# Перезапуск
sudo systemctl restart nginx

# Логи
sudo tail -f /var/log/nginx/error.log
```

### Безопасность

```bash
# Статус firewall
sudo ufw status

# Статус Fail2Ban
sudo fail2ban-client status

# Заблокированные IP
sudo fail2ban-client status sshd
```

---

## 🛠️ Настройка приложения

### Изменить конфигурацию

```bash
nano /home/deploy/app/.env
```

**Обязательно измените:**
- `SECRET_KEY` - на случайную строку

**Опционально:**
- `PORT` - если хотите другой порт
- `LOG_LEVEL` - уровень логирования

**После изменений:**

```bash
sudo systemctl restart tournaments
```

### Генерация секретного ключа

```bash
python3 -c 'import secrets; print(secrets.token_hex(32))'
```

---

## 🔍 Решение проблем

### Приложение не запускается

```bash
# Проверить логи
sudo journalctl -u tournaments -n 100 --no-pager

# Проверить конфигурацию Nginx
sudo nginx -t

# Проверить порты
sudo netstat -tulpn | grep :5000
```

### 502 Bad Gateway

```bash
# Проверить статус приложения
sudo systemctl status tournaments

# Перезапустить
sudo systemctl restart tournaments
```

### База данных не работает

```bash
cd /home/deploy/app
source venv/bin/activate
python init_db.py
```

---

## 📁 Структура файлов

```
/home/deploy/
├── app/                      # Приложение
│   ├── venv/                # Python окружение
│   ├── instance/            # База данных
│   ├── .env                 # Конфигурация (не в Git!)
│   └── deployment/          # Скрипты деплоя
├── logs/                    # Логи
└── backups/                 # Бэкапы БД
```

---

## 📞 Что дальше?

1. **Настроить SSL** - если есть домен
2. **Настроить автодеплой** - через webhook
3. **Настроить резервное копирование** - БД
4. **Изменить admin пароль** - в веб-интерфейсе
5. **Настроить email** - для уведомлений (если нужно)

---

## 🎯 Итого

- ✅ Сервер настроен и защищен
- ✅ Приложение установлено и запущено
- ✅ Nginx настроен как reverse proxy
- ✅ Автозапуск при перезагрузке
- ✅ Автоматические обновления безопасности
- ✅ Firewall и Fail2Ban защищают сервер

**Время полной установки: ~15 минут**

Полная документация: [README.md](README.md)

