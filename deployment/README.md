# 🚀 Deployment Guide - Quick Score Tournaments

Полное руководство по развертыванию приложения на VDS сервере Ubuntu без использования Docker.

## 📋 Содержание

1. [Требования](#требования)
2. [Подготовка сервера](#подготовка-сервера)
3. [Установка зависимостей](#установка-зависимостей)
4. [Деплой приложения](#деплой-приложения)
5. [Настройка автозапуска](#настройка-автозапуска)
6. [SSL сертификаты](#ssl-сертификаты)
7. [Обновление приложения](#обновление-приложения)

---

## 🖥️ Требования

### Сервер
- **OS**: Ubuntu 22.04 LTS или новее
- **RAM**: минимум 1GB (рекомендуется 2GB+)
- **Storage**: минимум 10GB
- **CPU**: 1 vCPU+

### Доступы
- Root доступ или sudo пользователь
- SSH ключ для доступа
- Домен (опционально, для SSL)

---

## 🔧 Подготовка сервера

### Шаг 1: Запуск автоматической настройки

Скопируйте скрипт на сервер и запустите:

```bash
# Скачать репозиторий
git clone https://github.com/ValeryMukhin1712/quick-score.git
cd quick-score/deployment

# Дать права на выполнение
chmod +x scripts/*.sh

# Запустить полную настройку сервера
sudo ./scripts/setup_server.sh
```

Этот скрипт выполнит:
- ✅ Обновление системы
- ✅ Установку необходимых пакетов
- ✅ Создание пользователя deploy
- ✅ Настройку SSH безопасности
- ✅ Установку firewall (UFW)
- ✅ Установку Fail2Ban
- ✅ Установку Python 3.11+
- ✅ Установку Nginx
- ✅ Настройку автообновлений

### Шаг 2: Настройка SSH (важно!)

После выполнения скрипта:

1. **Скопируйте ваш SSH ключ на сервер:**
```bash
ssh-copy-id -i ~/.ssh/id_rsa.pub deploy@YOUR_SERVER_IP
```

2. **Проверьте подключение:**
```bash
ssh deploy@YOUR_SERVER_IP
```

3. **Отключите пароль для root** (если еще не сделано):
```bash
sudo nano /etc/ssh/sshd_config
# Установите: PermitRootLogin no
sudo systemctl restart sshd
```

---

## 📦 Установка зависимостей

Скрипт установки уже всё настроил, но если нужно вручную:

```bash
# Python и инструменты
sudo apt install -y python3.11 python3.11-venv python3-pip

# Nginx
sudo apt install -y nginx

# Git
sudo apt install -y git

# Другие утилиты
sudo apt install -y htop curl wget unzip
```

---

## 🚢 Деплой приложения

### Автоматический деплой (рекомендуется)

```bash
# От пользователя deploy
cd /home/deploy
./quick-score/deployment/scripts/deploy_app.sh
```

### Ручной деплой

```bash
# 1. Клонировать репозиторий
cd /home/deploy
git clone https://github.com/ValeryMukhin1712/quick-score.git app

# 2. Создать виртуальное окружение
cd app
python3 -m venv venv
source venv/bin/activate

# 3. Установить зависимости
pip install --upgrade pip
pip install -r requirements.txt
pip install gunicorn

# 4. Создать .env файл
cp .env.example .env
nano .env  # Отредактировать настройки

# 5. Инициализировать базу данных
python init_db.py

# 6. Настроить Nginx
sudo cp deployment/nginx/tournaments.conf /etc/nginx/sites-available/
sudo ln -s /etc/nginx/sites-available/tournaments.conf /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# 7. Настроить systemd сервис
sudo cp deployment/systemd/tournaments.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable tournaments
sudo systemctl start tournaments
```

---

## ⚙️ Настройка автозапуска

Systemd сервис уже настроен автоматически. Проверка:

```bash
# Статус сервиса
sudo systemctl status tournaments

# Логи
sudo journalctl -u tournaments -f

# Перезапуск
sudo systemctl restart tournaments

# Остановка
sudo systemctl stop tournaments
```

---

## 🔒 SSL сертификаты (Let's Encrypt)

### Автоматическая установка:

```bash
# Установить Certbot
sudo apt install -y certbot python3-certbot-nginx

# Получить сертификат (замените YOUR_DOMAIN)
sudo certbot --nginx -d YOUR_DOMAIN.com -d www.YOUR_DOMAIN.com

# Автообновление сертификата
sudo certbot renew --dry-run
```

Certbot автоматически настроит Nginx для HTTPS.

---

## 🔄 Обновление приложения

### Простое обновление:

```bash
cd /home/deploy/app
git pull origin main
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart tournaments
```

### С помощью скрипта:

```bash
./deployment/scripts/deploy_app.sh --update
```

---

## 📊 Мониторинг

### Проверка работы приложения:

```bash
# Статус сервиса
sudo systemctl status tournaments

# Логи приложения
sudo journalctl -u tournaments -f --lines=100

# Логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Использование ресурсов
htop
```

---

## 🛡️ Безопасность

Скрипт автоматически настроил:

1. **UFW Firewall** - открыты только порты 22, 80, 443
2. **Fail2Ban** - защита от брутфорса SSH
3. **SSH** - только ключи, отключен root
4. **Автообновления** - критические патчи безопасности

### Дополнительно рекомендуется:

```bash
# Регулярные обновления
sudo apt update && sudo apt upgrade -y

# Проверка открытых портов
sudo netstat -tulpn

# Проверка активных соединений
sudo ss -tuln
```

---

## 🆘 Решение проблем

### Приложение не запускается:

```bash
# Проверить логи
sudo journalctl -u tournaments -n 50

# Проверить конфигурацию Nginx
sudo nginx -t

# Проверить порты
sudo netstat -tulpn | grep :5000
```

### База данных не создается:

```bash
cd /home/deploy/app
source venv/bin/activate
python init_db.py
```

### Nginx возвращает 502:

```bash
# Проверить, запущен ли gunicorn
ps aux | grep gunicorn

# Перезапустить сервис
sudo systemctl restart tournaments
```

---

## 📞 Поддержка

Если возникли проблемы, проверьте:
1. Логи systemd: `sudo journalctl -u tournaments -f`
2. Логи Nginx: `/var/log/nginx/error.log`
3. Логи приложения: `/home/deploy/app/app.log`

---

## 📝 Структура развертывания

```
/home/deploy/
├── app/                    # Основное приложение
│   ├── venv/              # Виртуальное окружение
│   ├── instance/          # База данных SQLite
│   ├── static/            # Статические файлы
│   ├── templates/         # Шаблоны HTML
│   ├── .env               # Конфигурация (НЕ в Git!)
│   └── app.py             # Главный файл
├── logs/                  # Логи (опционально)
└── backups/               # Бэкапы БД (опционально)
```

---

## ⚡ Быстрый старт (TL;DR)

```bash
# На сервере (Ubuntu 22.04)
git clone https://github.com/ValeryMukhin1712/quick-score.git
cd quick-score/deployment
chmod +x scripts/*.sh
sudo ./scripts/setup_server.sh
./scripts/deploy_app.sh
```

Готово! Приложение доступно по IP сервера или домену.

