# 🔐 Пошаговая настройка VDS сервера Ubuntu

## Начальная информация

Перед началом у вас должно быть:
- ✅ IP адрес сервера
- ✅ Root пароль для первого входа
- ✅ SSH клиент (Windows: PuTTY или встроенный OpenSSH, Linux/Mac: терминал)

---

## 📝 Шаг 1: Первое подключение к серверу

### Из PowerShell/CMD (Windows) или терминала (Linux/Mac):

```bash
ssh root@YOUR_SERVER_IP
```

Введите пароль root при запросе.

**Если подключение успешно, вы увидите приглашение:**
```
root@hostname:~#
```

---

## 🔒 Шаг 2: Смена пароля root (рекомендуется)

```bash
passwd
```

Введите новый сложный пароль (минимум 12 символов, буквы, цифры, спецсимволы).

**Сохраните новый пароль в надежном месте!**

---

## 👤 Шаг 3: Создание пользователя для администрирования

```bash
# Создать пользователя deploy
adduser deploy
```

Введите пароль для пользователя deploy (сохраните его!).
Остальные поля можно оставить пустыми (нажать Enter).

**Добавить пользователя в группу sudo:**

```bash
usermod -aG sudo deploy
```

**Проверить:**

```bash
groups deploy
```

Должно показать: `deploy : deploy sudo`

---

## 🔑 Шаг 4: Настройка SSH ключей (на локальном компьютере)

### 4.1 Генерация SSH ключа (если еще нет)

**На вашем ЛОКАЛЬНОМ компьютере (Windows PowerShell):**

```powershell
# Проверить, есть ли уже ключ
ls ~\.ssh\id_rsa.pub

# Если ключа нет, создать новый
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

При запросе:
- **Путь**: нажмите Enter (использовать по умолчанию)
- **Passphrase**: можно оставить пустым или ввести дополнительный пароль

### 4.2 Копирование ключа на сервер

**Вариант A: Автоматически (рекомендуется):**

```powershell
# На локальном компьютере
type ~\.ssh\id_rsa.pub | ssh deploy@YOUR_SERVER_IP "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

Введите пароль пользователя deploy.

**Вариант B: Вручную:**

1. На локальном компьютере скопировать содержимое ключа:
   ```powershell
   type ~\.ssh\id_rsa.pub
   ```
   Скопируйте весь текст (начинается с `ssh-rsa`).

2. На сервере (от root):
   ```bash
   # Создать директорию для SSH
   mkdir -p /home/deploy/.ssh
   
   # Создать файл authorized_keys
   nano /home/deploy/.ssh/authorized_keys
   ```
   
3. Вставьте скопированный ключ, сохраните (Ctrl+O, Enter, Ctrl+X).

4. Установить правильные права:
   ```bash
   chmod 700 /home/deploy/.ssh
   chmod 600 /home/deploy/.ssh/authorized_keys
   chown -R deploy:deploy /home/deploy/.ssh
   ```

### 4.3 Проверка SSH ключа

**Откройте НОВОЕ окно терминала** (не закрывайте старое!) и попробуйте:

```bash
ssh deploy@YOUR_SERVER_IP
```

Если всё настроено правильно, вы войдете **БЕЗ ПАРОЛЯ**.

⚠️ **НЕ ЗАКРЫВАЙТЕ старое окно с root до проверки!**

---

## 🔐 Шаг 5: Настройка SSH безопасности

**На сервере (от пользователя deploy с sudo):**

### 5.1 Создать конфигурацию SSH

```bash
sudo nano /etc/ssh/sshd_config.d/security.conf
```

Вставьте:

```
# SSH Security Settings
Port 22
PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes
ChallengeResponseAuthentication no
UsePAM yes
X11Forwarding no
PrintMotd no
AcceptEnv LANG LC_*
ClientAliveInterval 300
ClientAliveCountMax 2
MaxAuthTries 3
MaxSessions 10
```

Сохраните (Ctrl+O, Enter, Ctrl+X).

### 5.2 Проверка конфигурации SSH

```bash
sudo sshd -t
```

Если нет ошибок, продолжайте.

### 5.3 Перезапуск SSH (ОСТОРОЖНО!)

⚠️ **ВАЖНО: Проверьте, что вы можете войти как deploy БЕЗ ПАРОЛЯ!**

Если проверка успешна:

```bash
sudo systemctl restart sshd
```

**Проверка:** Откройте новое окно терминала и попробуйте:
```bash
ssh deploy@YOUR_SERVER_IP
```

Должно войти без пароля.

**Попробуйте войти как root (должно быть запрещено):**
```bash
ssh root@YOUR_SERVER_IP
```

Должно показать: `Permission denied`

---

## 🛡️ Шаг 6: Автоматическая настройка остального (firewall, fail2ban и т.д.)

Теперь можно запустить автоматический скрипт настройки:

```bash
# Клонировать репозиторий
cd ~
git clone https://github.com/ValeryMukhin1712/quick-score.git
cd quick-score/deployment

# Дать права на выполнение
chmod +x scripts/*.sh

# Запустить настройку сервера
sudo ./scripts/setup_server.sh
```

Этот скрипт настроит:
- ✅ Обновление системы
- ✅ Python, Nginx, Git
- ✅ UFW Firewall
- ✅ Fail2Ban
- ✅ Автообновления безопасности

---

## 📋 Проверочный лист безопасности

После всех шагов проверьте:

- [ ] Вы можете войти как `deploy` БЕЗ пароля (SSH ключ)
- [ ] Вы НЕ можете войти как `root` (запрещено)
- [ ] Вход по паролю отключен (только SSH ключи)
- [ ] Firewall включен и настроен
- [ ] Fail2Ban запущен

**Команды для проверки:**

```bash
# Статус SSH
sudo systemctl status sshd

# Статус Firewall
sudo ufw status

# Статус Fail2Ban
sudo systemctl status fail2ban

# Открытые порты
sudo ss -tulpn | grep LISTEN
```

---

## 🆘 Что делать если что-то пошло не так

### Не могу войти после изменения SSH конфига

1. Войдите через веб-консоль вашего хостинг-провайдера (VNC/Console)
2. Войдите как root (локально пароль работает)
3. Отмените изменения:
   ```bash
   rm /etc/ssh/sshd_config.d/security.conf
   systemctl restart sshd
   ```
4. Повторите настройку заново

### Забыл пароль deploy

1. Войдите через веб-консоль как root
2. Смените пароль:
   ```bash
   passwd deploy
   ```

### Потерял SSH ключ

1. Войдите через веб-консоль как root
2. Создайте новый ключ на локальном компьютере
3. Повторите Шаг 4 (копирование ключа)

---

## 🎯 Следующие шаги после настройки безопасности

После успешной настройки безопасности:

1. ✅ Запустить скрипт деплоя приложения:
   ```bash
   cd ~/quick-score/deployment
   ./scripts/deploy_app.sh
   ```

2. ✅ Настроить домен (если есть)

3. ✅ Установить SSL сертификат

---

## 💡 Полезные команды

```bash
# Просмотр логов входа
sudo tail -f /var/log/auth.log

# Список активных сессий
who

# История команд
history

# Информация о системе
uname -a
lsb_release -a
df -h
free -h
```

---

**Готово! Сервер защищен и готов к деплою приложения.** 🎉

